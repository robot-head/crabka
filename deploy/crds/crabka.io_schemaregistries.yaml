apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: schemaregistries.crabka.io
spec:
  group: crabka.io
  names:
    kind: SchemaRegistry
    plural: schemaregistries
    shortNames:
    - sr
    singular: schemaregistry
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for SchemaRegistrySpec via `CustomResource`
        properties:
          spec:
            properties:
              authentication:
                description: REST authentication.
                nullable: true
                properties:
                  basic:
                    nullable: true
                    properties:
                      usersSecretKey:
                        description: Secret key holding the htpasswd-style file. Default `users`.
                        nullable: true
                        type: string
                      usersSecretName:
                        description: |-
                          Secret with a single key holding newline-separated `user:cred`
                          entries (cred = plaintext or `$2…` bcrypt). The key is mounted as
                          a file and passed via `--basic-auth-file`.
                        type: string
                    required:
                    - usersSecretName
                    type: object
                  bearer:
                    nullable: true
                    properties:
                      jwksEndpointUri:
                        description: JWKS endpoint URI (required when `mode` = `Jwks`).
                        nullable: true
                        type: string
                      jwksExpectedAudience:
                        description: Expected `aud` claim value.
                        nullable: true
                        type: string
                      jwksPrincipalClaim:
                        description: |-
                          JWT claim to use as the principal when mode is `Jwks`. Overrides
                          `principalClaim` for JWKS paths.
                        nullable: true
                        type: string
                      jwksRefreshMs:
                        description: JWKS key-set refresh interval in milliseconds. Default 60 000.
                        format: int64
                        nullable: true
                        type: integer
                      jwksTlsSecretName:
                        description: |-
                          Secret name whose `ca.crt` key is mounted and passed as
                          `--bearer-jwks-ca`.
                        nullable: true
                        type: string
                      jwksValidIssuer:
                        description: Expected `iss` claim value.
                        nullable: true
                        type: string
                      mode:
                        enum:
                        - Unsecured
                        - Jwks
                        type: string
                      principalClaim:
                        description: JWT claim used as the principal name. Default `sub`.
                        nullable: true
                        type: string
                    required:
                    - mode
                    type: object
                  realm:
                    description: '`WWW-Authenticate: basic realm="<realm>"`.'
                    nullable: true
                    type: string
                  requireAuth:
                    default: false
                    description: Reject anonymous requests with 401.
                    type: boolean
                type: object
              authorization:
                description: REST authorization (Kafka-ACL based).
                nullable: true
                properties:
                  aclRefreshSeconds:
                    description: ACL-cache refresh interval (seconds). Default 30.
                    format: int64
                    nullable: true
                    type: integer
                  enabled:
                    default: false
                    type: boolean
                  superUsers:
                    items:
                      type: string
                    type: array
                type: object
              bootstrapServers:
                description: |-
                  Override bootstrap for an external/unmanaged Kafka. When unset,
                  bootstrap is derived from the `crabka.io/cluster`-labeled Kafka's
                  internal listener. (Secured external brokers are a future
                  enhancement; the managed/label path is the secured one.)
                nullable: true
                type: string
              clientId:
                description: Kafka client id used by the registry. Default `crabka-schema-registry`.
                minLength: 1
                nullable: true
                type: string
              groupId:
                description: Election group id. Default `schema-registry`.
                nullable: true
                type: string
              healthChecks:
                description: Kubernetes probe timing overrides.
                nullable: true
                properties:
                  livenessInitialDelaySeconds:
                    format: int32
                    minimum: 0.0
                    nullable: true
                    type: integer
                  livenessPeriodSeconds:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                  readinessInitialDelaySeconds:
                    format: int32
                    minimum: 0.0
                    nullable: true
                    type: integer
                  readinessPeriodSeconds:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                type: object
              image:
                description: |-
                  Container image. Defaults to the operator's
                  `--default-schema-registry-image`.
                nullable: true
                type: string
              kafkaClient:
                description: SR → broker client security (SASL / TLS). Maps to `--kafka-*` flags.
                nullable: true
                properties:
                  sasl:
                    description: SASL credentials for the SR → broker connection.
                    nullable: true
                    properties:
                      mechanism:
                        description: e.g. `PLAIN`, `SCRAM-SHA-256`, `SCRAM-SHA-512`.
                        type: string
                      secretRef:
                        description: Name of the Secret holding `username` and `password` keys.
                        type: string
                    required:
                    - mechanism
                    - secretRef
                    type: object
                  securityProtocol:
                    description: e.g. `PLAINTEXT`, `SASL_PLAINTEXT`, `SSL`, `SASL_SSL`.
                    nullable: true
                    type: string
                  tls:
                    description: TLS settings for the SR → broker connection.
                    nullable: true
                    properties:
                      caSecretName:
                        description: Secret with a `ca.crt` key used as the broker CA.
                        nullable: true
                        type: string
                      serverNameOverride:
                        description: Override the server name used for TLS SNI / hostname verification.
                        nullable: true
                        type: string
                    type: object
                type: object
              replicas:
                description: Stateless replicas; all join the election group. Default 1.
                format: int32
                maximum: 1000.0
                minimum: 1.0
                type: integer
              resources:
                description: Pod resource requirements.
                nullable: true
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container.

                      This is an alpha field and requires enabling the DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container.
                          type: string
                        request:
                          description: Request is the name chosen for a request in the referenced claim. If empty, everything from the claim is made available, otherwise only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  limits:
                    additionalProperties:
                      description: "Quantity is a fixed-point representation of a number. It provides convenient marshaling/unmarshaling in JSON and YAML, in addition to String() and AsInt64() accessors.\n\nThe serialization format is:\n\n``` <quantity>        ::= <signedNumber><suffix>\n\n\t(Note that <suffix> may be empty, from the \"\" case in <decimalSI>.)\n\n<digit>           ::= 0 | 1 | ... | 9 <digits>          ::= <digit> | <digit><digits> <number>          ::= <digits> | <digits>.<digits> | <digits>. | .<digits> <sign>            ::= \"+\" | \"-\" <signedNumber>    ::= <number> | <sign><number> <suffix>          ::= <binarySI> | <decimalExponent> | <decimalSI> <binarySI>        ::= Ki | Mi | Gi | Ti | Pi | Ei\n\n\t(International System of units; See: http://physics.nist.gov/cuu/Units/binary.html)\n\n<decimalSI>       ::= m | \"\" | k | M | G | T | P | E\n\n\t(Note that 1024 = 1Ki but 1000 = 1k; I didn't choose the capitalization.)\n\n<decimalExponent> ::= \"e\" <signedNumber> | \"E\" <signedNumber> ```\n\nNo matter which of the three exponent forms is used, no quantity may represent a number greater than 2^63-1 in magnitude, nor may it have more than 3 decimal places. Numbers larger or more precise will be capped or rounded up. (E.g.: 0.1m will rounded up to 1m.) This may be extended in the future if we require larger or smaller quantities.\n\nWhen a Quantity is parsed from a string, it will remember the type of suffix it had, and will use the same type again when it is serialized.\n\nBefore serializing, Quantity will be put in \"canonical form\". This means that Exponent/suffix will be adjusted up or down (with a corresponding increase or decrease in Mantissa) such that:\n\n- No precision is lost - No fractional digits will be emitted - The exponent (or suffix) is as large as possible.\n\nThe sign will be omitted unless the number is negative.\n\nExamples:\n\n- 1.5 will be serialized as \"1500m\" - 1.5Gi will be serialized as \"1536Mi\"\n\nNote that the quantity will NEVER be internally represented by a floating point number. That is the whole point of this exercise.\n\nNon-canonical values will still parse as long as they are well formed, but will be re-emitted in their canonical form. (So always use canonical form, or don't diff.)\n\nThis format is intended to make it difficult to use these numbers without writing some sort of special handling code in the hopes that that will cause implementors to also use a fixed point implementation."
                      x-kubernetes-int-or-string: true
                    description: 'Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  requests:
                    additionalProperties:
                      description: "Quantity is a fixed-point representation of a number. It provides convenient marshaling/unmarshaling in JSON and YAML, in addition to String() and AsInt64() accessors.\n\nThe serialization format is:\n\n``` <quantity>        ::= <signedNumber><suffix>\n\n\t(Note that <suffix> may be empty, from the \"\" case in <decimalSI>.)\n\n<digit>           ::= 0 | 1 | ... | 9 <digits>          ::= <digit> | <digit><digits> <number>          ::= <digits> | <digits>.<digits> | <digits>. | .<digits> <sign>            ::= \"+\" | \"-\" <signedNumber>    ::= <number> | <sign><number> <suffix>          ::= <binarySI> | <decimalExponent> | <decimalSI> <binarySI>        ::= Ki | Mi | Gi | Ti | Pi | Ei\n\n\t(International System of units; See: http://physics.nist.gov/cuu/Units/binary.html)\n\n<decimalSI>       ::= m | \"\" | k | M | G | T | P | E\n\n\t(Note that 1024 = 1Ki but 1000 = 1k; I didn't choose the capitalization.)\n\n<decimalExponent> ::= \"e\" <signedNumber> | \"E\" <signedNumber> ```\n\nNo matter which of the three exponent forms is used, no quantity may represent a number greater than 2^63-1 in magnitude, nor may it have more than 3 decimal places. Numbers larger or more precise will be capped or rounded up. (E.g.: 0.1m will rounded up to 1m.) This may be extended in the future if we require larger or smaller quantities.\n\nWhen a Quantity is parsed from a string, it will remember the type of suffix it had, and will use the same type again when it is serialized.\n\nBefore serializing, Quantity will be put in \"canonical form\". This means that Exponent/suffix will be adjusted up or down (with a corresponding increase or decrease in Mantissa) such that:\n\n- No precision is lost - No fractional digits will be emitted - The exponent (or suffix) is as large as possible.\n\nThe sign will be omitted unless the number is negative.\n\nExamples:\n\n- 1.5 will be serialized as \"1500m\" - 1.5Gi will be serialized as \"1536Mi\"\n\nNote that the quantity will NEVER be internally represented by a floating point number. That is the whole point of this exercise.\n\nNon-canonical values will still parse as long as they are well formed, but will be re-emitted in their canonical form. (So always use canonical form, or don't diff.)\n\nThis format is intended to make it difficult to use these numbers without writing some sort of special handling code in the hopes that that will cause implementors to also use a fixed point implementation."
                      x-kubernetes-int-or-string: true
                    description: 'Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                type: object
              runtime:
                description: Schema Registry runtime policy.
                nullable: true
                properties:
                  defaultCompatibilityLevel:
                    nullable: true
                    type: string
                  defaultMode:
                    nullable: true
                    type: string
                  electionHeartbeatIntervalMs:
                    format: uint64
                    minimum: 1.0
                    nullable: true
                    type: integer
                  electionRebalanceTimeoutMs:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                  electionReconnectBackoffMs:
                    format: uint64
                    minimum: 1.0
                    nullable: true
                    type: integer
                  electionSessionTimeoutMs:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                  schemasTopicCreateTimeoutMs:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                  storeReaderFetchMaxBytes:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                  storeReaderFetchMaxWaitMs:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                  storeReaderRetryBackoffMs:
                    format: uint64
                    minimum: 1.0
                    nullable: true
                    type: integer
                type: object
              schemasTopic:
                description: Backing compacted topic. Default `_schemas`.
                nullable: true
                type: string
              schemasTopicReplicationFactor:
                description: Replication factor for `_schemas` when auto-created. Default 3.
                format: int32
                nullable: true
                type: integer
              tls:
                description: Server TLS (HTTPS REST). None = plain HTTP.
                nullable: true
                properties:
                  clientAuth:
                    description: Client-cert mode. Default `Disabled`.
                    enum:
                    - Disabled
                    - Optional
                    - Required
                    - null
                    nullable: true
                    type: string
                  clientCaSecretName:
                    description: |-
                      Secret with `ca.crt` to verify client certs (required when
                      `clientAuth` != Disabled).
                    nullable: true
                    type: string
                  issuerRef:
                    description: cert-manager issuer reference. Mutually exclusive with `secretName`.
                    nullable: true
                    properties:
                      group:
                        description: API group. Default `cert-manager.io`.
                        nullable: true
                        type: string
                      kind:
                        description: Defaults to `Issuer`; set `ClusterIssuer` for cluster-scoped issuers.
                        nullable: true
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  secretName:
                    description: |-
                      Secret (type kubernetes.io/tls) with `tls.crt` + `tls.key`.
                      Mutually exclusive with `issuerRef`.
                    nullable: true
                    type: string
                type: object
            required:
            - replicas
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                description: 'Kubernetes-style conditions: `KafkaReady`, `Available`, `Ready`.'
                items:
                  properties:
                    lastTransitionTime:
                      description: RFC3339 timestamp.
                      type: string
                    message:
                      description: Human-readable message.
                      type: string
                    reason:
                      description: CamelCase machine reason.
                      type: string
                    status:
                      description: '`True`, `False`, or `Unknown`.'
                      type: string
                    type:
                      description: e.g. `Ready`.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: '`metadata.generation` of the last successfully-reconciled spec.'
                format: int64
                nullable: true
                type: integer
              readyReplicas:
                format: int32
                nullable: true
                type: integer
              replicas:
                format: int32
                nullable: true
                type: integer
              url:
                description: In-cluster REST URL clients use.
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: SchemaRegistry
        type: object
    served: true
    storage: true
    subresources:
      status: {}
