apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: schemaregistries.crabka.io
spec:
  group: crabka.io
  names:
    kind: SchemaRegistry
    plural: schemaregistries
    shortNames:
    - sr
    singular: schemaregistry
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for SchemaRegistrySpec via `CustomResource`
        properties:
          spec:
            properties:
              authentication:
                description: REST authentication.
                nullable: true
                properties:
                  basic:
                    nullable: true
                    properties:
                      usersSecretKey:
                        description: Secret key holding the htpasswd-style file. Default `users`.
                        nullable: true
                        type: string
                      usersSecretName:
                        description: |-
                          Secret with one key that holds `user:cred` entries, one on each
                          line. The cred is plaintext or a `$2…` bcrypt hash. The operator
                          mounts the key as a file and gives it in `--basic-auth-file`.
                        type: string
                    required:
                    - usersSecretName
                    type: object
                  bearer:
                    nullable: true
                    properties:
                      jwksEndpointUri:
                        description: JWKS endpoint URI. It is necessary when `mode` is `Jwks`.
                        nullable: true
                        type: string
                      jwksExpectedAudience:
                        description: Expected `aud` claim value.
                        nullable: true
                        type: string
                      jwksPrincipalClaim:
                        description: |-
                          JWT claim to use as the principal when the mode is `Jwks`. It
                          overrides `principalClaim` on the JWKS paths.
                        nullable: true
                        type: string
                      jwksRefresh:
                        description: JWKS key-set refresh interval. Default `1m`.
                        nullable: true
                        type: string
                      jwksTlsSecretName:
                        description: |-
                          Name of the Secret whose `ca.crt` key the operator mounts and
                          gives in `--bearer-jwks-ca`.
                        nullable: true
                        type: string
                      jwksValidIssuer:
                        description: Expected `iss` claim value.
                        nullable: true
                        type: string
                      mode:
                        enum:
                        - Unsecured
                        - Jwks
                        type: string
                      principalClaim:
                        description: JWT claim used as the principal name. Default `sub`.
                        nullable: true
                        type: string
                    required:
                    - mode
                    type: object
                  realm:
                    description: '`WWW-Authenticate: basic realm="<realm>"`.'
                    nullable: true
                    type: string
                  requireAuth:
                    default: false
                    description: Reject anonymous requests with 401.
                    type: boolean
                type: object
              authorization:
                description: REST authorization, based on the Kafka ACLs.
                nullable: true
                properties:
                  aclRefresh:
                    description: ACL-cache refresh interval. Default `30s`.
                    nullable: true
                    type: string
                  enabled:
                    default: false
                    type: boolean
                  superUsers:
                    items:
                      type: string
                    type: array
                type: object
              bootstrapServers:
                description: |-
                  Bootstrap override for an external Kafka that the operator does
                  not manage. When unset, the operator derives the bootstrap from the
                  internal listener of the Kafka with the `crabka.io/cluster` label.
                  Settings in `kafka_client` secure both external and managed bootstrap
                  connections.
                nullable: true
                type: string
              clientId:
                description: Kafka client id used by the registry. Default `crabka-schema-registry`.
                minLength: 1
                nullable: true
                type: string
              groupId:
                description: Election group id. Default `schema-registry`.
                nullable: true
                type: string
              healthChecks:
                description: Kubernetes probe timing overrides.
                nullable: true
                properties:
                  livenessInitialDelaySeconds:
                    format: int32
                    minimum: 0.0
                    nullable: true
                    type: integer
                  livenessPeriodSeconds:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                  readinessInitialDelaySeconds:
                    format: int32
                    minimum: 0.0
                    nullable: true
                    type: integer
                  readinessPeriodSeconds:
                    format: int32
                    minimum: 1.0
                    nullable: true
                    type: integer
                type: object
              image:
                description: |-
                  Container image. Defaults to the operator's
                  `--default-schema-registry-image`.
                nullable: true
                type: string
              kafkaClient:
                description: |-
                  Client security from the SR to the broker, with SASL and TLS. It
                  maps to the `--kafka-*` flags.
                nullable: true
                properties:
                  sasl:
                    description: SASL credentials for the connection from the SR to the broker.
                    nullable: true
                    properties:
                      mechanism:
                        description: For example `PLAIN`, `SCRAM-SHA-256`, or `SCRAM-SHA-512`.
                        type: string
                      secretRef:
                        description: Name of the Secret that holds the `username` and `password` keys.
                        type: string
                    required:
                    - mechanism
                    - secretRef
                    type: object
                  securityProtocol:
                    description: For example `PLAINTEXT`, `SASL_PLAINTEXT`, `SSL`, or `SASL_SSL`.
                    nullable: true
                    type: string
                  tls:
                    description: TLS settings for the connection from the SR to the broker.
                    nullable: true
                    properties:
                      caSecretName:
                        description: Secret with a `ca.crt` key that gives the broker CA.
                        nullable: true
                        type: string
                      serverNameOverride:
                        description: Override the server name for TLS SNI and hostname verification.
                        nullable: true
                        type: string
                    type: object
                type: object
              replicas:
                description: |-
                  Number of stateless replicas. All of them join the election
                  group. Default 1.
                format: int32
                maximum: 1000.0
                minimum: 1.0
                type: integer
              resources:
                description: Pod resource requirements.
                nullable: true
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container.

                      This is an alpha field and requires enabling the DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container.
                          type: string
                        request:
                          description: Request is the name chosen for a request in the referenced claim. If empty, everything from the claim is made available, otherwise only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  limits:
                    additionalProperties:
                      description: "Quantity is a fixed-point representation of a number. It provides convenient marshaling/unmarshaling in JSON and YAML, in addition to String() and AsInt64() accessors.\n\nThe serialization format is:\n\n``` <quantity>        ::= <signedNumber><suffix>\n\n\t(Note that <suffix> may be empty, from the \"\" case in <decimalSI>.)\n\n<digit>           ::= 0 | 1 | ... | 9 <digits>          ::= <digit> | <digit><digits> <number>          ::= <digits> | <digits>.<digits> | <digits>. | .<digits> <sign>            ::= \"+\" | \"-\" <signedNumber>    ::= <number> | <sign><number> <suffix>          ::= <binarySI> | <decimalExponent> | <decimalSI> <binarySI>        ::= Ki | Mi | Gi | Ti | Pi | Ei\n\n\t(International System of units; See: http://physics.nist.gov/cuu/Units/binary.html)\n\n<decimalSI>       ::= m | \"\" | k | M | G | T | P | E\n\n\t(Note that 1024 = 1Ki but 1000 = 1k; I didn't choose the capitalization.)\n\n<decimalExponent> ::= \"e\" <signedNumber> | \"E\" <signedNumber> ```\n\nNo matter which of the three exponent forms is used, no quantity may represent a number greater than 2^63-1 in magnitude, nor may it have more than 3 decimal places. Numbers larger or more precise will be capped or rounded up. (E.g.: 0.1m will rounded up to 1m.) This may be extended in the future if we require larger or smaller quantities.\n\nWhen a Quantity is parsed from a string, it will remember the type of suffix it had, and will use the same type again when it is serialized.\n\nBefore serializing, Quantity will be put in \"canonical form\". This means that Exponent/suffix will be adjusted up or down (with a corresponding increase or decrease in Mantissa) such that:\n\n- No precision is lost - No fractional digits will be emitted - The exponent (or suffix) is as large as possible.\n\nThe sign will be omitted unless the number is negative.\n\nExamples:\n\n- 1.5 will be serialized as \"1500m\" - 1.5Gi will be serialized as \"1536Mi\"\n\nNote that the quantity will NEVER be internally represented by a floating point number. That is the whole point of this exercise.\n\nNon-canonical values will still parse as long as they are well formed, but will be re-emitted in their canonical form. (So always use canonical form, or don't diff.)\n\nThis format is intended to make it difficult to use these numbers without writing some sort of special handling code in the hopes that that will cause implementors to also use a fixed point implementation."
                      x-kubernetes-int-or-string: true
                    description: 'Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  requests:
                    additionalProperties:
                      description: "Quantity is a fixed-point representation of a number. It provides convenient marshaling/unmarshaling in JSON and YAML, in addition to String() and AsInt64() accessors.\n\nThe serialization format is:\n\n``` <quantity>        ::= <signedNumber><suffix>\n\n\t(Note that <suffix> may be empty, from the \"\" case in <decimalSI>.)\n\n<digit>           ::= 0 | 1 | ... | 9 <digits>          ::= <digit> | <digit><digits> <number>          ::= <digits> | <digits>.<digits> | <digits>. | .<digits> <sign>            ::= \"+\" | \"-\" <signedNumber>    ::= <number> | <sign><number> <suffix>          ::= <binarySI> | <decimalExponent> | <decimalSI> <binarySI>        ::= Ki | Mi | Gi | Ti | Pi | Ei\n\n\t(International System of units; See: http://physics.nist.gov/cuu/Units/binary.html)\n\n<decimalSI>       ::= m | \"\" | k | M | G | T | P | E\n\n\t(Note that 1024 = 1Ki but 1000 = 1k; I didn't choose the capitalization.)\n\n<decimalExponent> ::= \"e\" <signedNumber> | \"E\" <signedNumber> ```\n\nNo matter which of the three exponent forms is used, no quantity may represent a number greater than 2^63-1 in magnitude, nor may it have more than 3 decimal places. Numbers larger or more precise will be capped or rounded up. (E.g.: 0.1m will rounded up to 1m.) This may be extended in the future if we require larger or smaller quantities.\n\nWhen a Quantity is parsed from a string, it will remember the type of suffix it had, and will use the same type again when it is serialized.\n\nBefore serializing, Quantity will be put in \"canonical form\". This means that Exponent/suffix will be adjusted up or down (with a corresponding increase or decrease in Mantissa) such that:\n\n- No precision is lost - No fractional digits will be emitted - The exponent (or suffix) is as large as possible.\n\nThe sign will be omitted unless the number is negative.\n\nExamples:\n\n- 1.5 will be serialized as \"1500m\" - 1.5Gi will be serialized as \"1536Mi\"\n\nNote that the quantity will NEVER be internally represented by a floating point number. That is the whole point of this exercise.\n\nNon-canonical values will still parse as long as they are well formed, but will be re-emitted in their canonical form. (So always use canonical form, or don't diff.)\n\nThis format is intended to make it difficult to use these numbers without writing some sort of special handling code in the hopes that that will cause implementors to also use a fixed point implementation."
                      x-kubernetes-int-or-string: true
                    description: 'Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                type: object
              runtime:
                description: Schema Registry runtime policy.
                nullable: true
                properties:
                  clientDispatchQueueCapacity:
                    description: Kafka client request-dispatch queue capacity.
                    format: uint
                    minimum: 1.0
                    nullable: true
                    type: integer
                  clientFrameMax:
                    description: Maximum accepted Kafka client frame size.
                    nullable: true
                    type: string
                  defaultCompatibilityLevel:
                    nullable: true
                    type: string
                  defaultMode:
                    nullable: true
                    type: string
                  electionHeartbeatInterval:
                    nullable: true
                    type: string
                  electionRebalanceTimeout:
                    nullable: true
                    type: string
                  electionReconnectBackoff:
                    nullable: true
                    type: string
                  electionSessionTimeout:
                    nullable: true
                    type: string
                  forwardMaxBody:
                    nullable: true
                    type: string
                  schemasTopicCreateTimeout:
                    nullable: true
                    type: string
                  storeReaderFetchMax:
                    nullable: true
                    type: string
                  storeReaderFetchMaxWait:
                    nullable: true
                    type: string
                  storeReaderRetryBackoff:
                    nullable: true
                    type: string
                type: object
              schemasTopic:
                description: Backing compacted topic. Default `_schemas`.
                nullable: true
                type: string
              schemasTopicReplicationFactor:
                description: Replication factor for `_schemas` when auto-created. Default 3.
                format: int32
                nullable: true
                type: integer
              tls:
                description: Server TLS for the HTTPS REST surface. `None` means plain HTTP.
                nullable: true
                properties:
                  clientAuth:
                    description: Client-cert mode. Default `Disabled`.
                    enum:
                    - Disabled
                    - Optional
                    - Required
                    - null
                    nullable: true
                    type: string
                  clientCaSecretName:
                    description: |-
                      Secret with `ca.crt` that verifies the client certs. It is
                      necessary when `clientAuth` is not Disabled.
                    nullable: true
                    type: string
                  issuerRef:
                    description: |-
                      cert-manager issuer reference. Do not set it together with
                      `secretName`.
                    nullable: true
                    properties:
                      group:
                        description: API group. Default `cert-manager.io`.
                        nullable: true
                        type: string
                      kind:
                        description: |-
                          The default is `Issuer`. Set `ClusterIssuer` for an issuer with
                          cluster scope.
                        nullable: true
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  secretName:
                    description: |-
                      Secret of type kubernetes.io/tls with `tls.crt` and `tls.key`. Do
                      not set it together with `issuerRef`.
                    nullable: true
                    type: string
                type: object
            required:
            - replicas
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                description: |-
                  Kubernetes-style conditions: `KafkaReady`, `Available`, and
                  `Ready`.
                items:
                  properties:
                    lastTransitionTime:
                      description: RFC3339 timestamp.
                      type: string
                    message:
                      description: Human-readable message.
                      type: string
                    reason:
                      description: CamelCase machine reason.
                      type: string
                    status:
                      description: '`True`, `False`, or `Unknown`.'
                      type: string
                    type:
                      description: For example, `Ready`.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: '`metadata.generation` of the last successfully-reconciled spec.'
                format: int64
                nullable: true
                type: integer
              readyReplicas:
                format: int32
                nullable: true
                type: integer
              replicas:
                format: int32
                nullable: true
                type: integer
              url:
                description: In-cluster REST URL that the clients use.
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: SchemaRegistry
        type: object
    served: true
    storage: true
    subresources:
      status: {}
