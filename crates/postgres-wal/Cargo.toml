[package]
name = "crabka-postgres-wal"
publish = false # Internal WAL decoder crate; keep out of the publish allowlist.
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Sans-IO PostgreSQL WAL decoder internals for Crabka"
repository = "https://github.com/robot-head/crabka"
homepage = "https://github.com/robot-head/crabka"
documentation = "https://docs.rs/crabka-postgres-wal"
keywords = ["postgres", "wal", "cdc", "crabka"]
categories = ["database"]

[lints]
workspace = true

[dependencies]
bytes = { workspace = true }
crc32c = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
assert2 = { workspace = true }
toml = { workspace = true }
