# crabka-sdk-conformance

`crabka-sdk-conformance` is the JSON-stdio contract harness for Crabka application SDKs.

The crate is private to the repository. It pins contract v1 protocol types, mock-validated vectors, and a CLI that can drive any language adapter executable.
