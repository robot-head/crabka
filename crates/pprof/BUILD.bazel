load("//:bazel/rust_package.bzl", "rust_package")

rust_package(
    name = "pprof",
    build_script = True,
    cargo_name = "crabka-pprof",
    crate_name = "crabka_pprof",
    lib_test_rustc_flags = ["-Clink-dead-code"],
    lib_test_size = "small",
)
