[package]
name = "crabka-postgres-redo"
publish = false # Internal PostgreSQL redo scaffold; keep out of the publish allowlist.
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Pure PostgreSQL redo scaffold for Crabka"
repository = "https://github.com/robot-head/crabka"
homepage = "https://github.com/robot-head/crabka"
documentation = "https://docs.rs/crabka-postgres-redo"
keywords = ["postgres", "redo", "wal", "crabka"]
categories = ["database"]

[lints]
workspace = true

[dependencies]
bytes = { workspace = true }
crabka-page-store = { path = "../page-store", version = "0.3.9" }
crabka-postgres-wal = { path = "../postgres-wal", version = "0.3.9" }
thiserror = { workspace = true }

[dev-dependencies]
assert2 = { workspace = true }
toml = { workspace = true }
