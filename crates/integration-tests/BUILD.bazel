load("//:bazel/rust_package.bzl", "rust_package")

rust_package(
    name = "integration-tests",
    cargo_name = "crabka-integration-tests",
    crate_name = "crabka_integration_tests",
    test_compile_data = ["//crates/broker:test_support"],
    test_sizes = {
        "grafana_e2e": "medium",
        "loki_differential": "large",
    },
    test_tags = {"diskless_jepsen": ["manual"]},
)
