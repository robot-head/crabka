load("//:bazel/rust_package.bzl", "rust_package")

rust_package(
    name = "client-core",
    cargo_name = "crabka-client-core",
    crate_name = "crabka_client_core",
    test_features = {"unit": ["mock"]},
)
