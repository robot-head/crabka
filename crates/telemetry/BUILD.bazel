load("//:bazel/rust_package.bzl", "rust_package")

rust_package(
    name = "telemetry",
    cargo_name = "crabka-telemetry",
    crate_name = "crabka_telemetry",
    extra_deps = ["@crates//:jemalloc_pprof"],
    features = ["heap-profiling"],
)
