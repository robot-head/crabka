// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use crate::primitives::fixed::{get_i8, get_i32, put_i8, put_i32};
use crate::tagged_fields::{
    WriteTaggedFields, encode_to_bytes, read_tagged_fields, tagged_fields_len,
};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};
use bytes::{Buf, BufMut};
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 2;
pub const FLEXIBLE_MIN: i16 = 0;
#[inline]
#[must_use]
pub fn is_flexible(version: i16) -> bool {
    version >= FLEXIBLE_MIN
}
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PartitionChangeRecord {
    pub partition_id: i32,
    pub topic_id: crate::primitives::uuid::Uuid,
    pub isr: Option<Vec<i32>>,
    pub leader: i32,
    pub replicas: Option<Vec<i32>>,
    pub removing_replicas: Option<Vec<i32>>,
    pub adding_replicas: Option<Vec<i32>>,
    pub leader_recovery_state: i8,
    pub directories: Option<Vec<crate::primitives::uuid::Uuid>>,
    pub eligible_leader_replicas: Option<Vec<i32>>,
    pub last_known_elr: Option<Vec<i32>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Default for PartitionChangeRecord {
    fn default() -> Self {
        Self {
            partition_id: -1i32,
            topic_id: crate::primitives::uuid::Uuid::default(),
            isr: None,
            leader: -2i32,
            replicas: None,
            removing_replicas: None,
            adding_replicas: None,
            leader_recovery_state: -1i8,
            directories: None,
            eligible_leader_replicas: None,
            last_known_elr: None,
            unknown_tagged_fields: UnknownTaggedFields::default(),
        }
    }
}
impl PartitionChangeRecord {
    fn encode_field_0<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            put_i32(buf, self.partition_id);
        }
    }
    fn encode_field_1<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            crate::primitives::uuid::put_uuid(buf, self.topic_id);
        }
    }
    fn encode_tagged_fields<B: BufMut>(&self, buf: &mut B, _version: i16, flex: bool) {
        if flex {
            let mut tagged = WriteTaggedFields::new();
            if self.isr.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.isr).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.isr).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.isr {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(0, payload);
            }
            if self.leader != -2i32 {
                let payload = encode_to_bytes(4, |b| {
                    put_i32(b, self.leader);
                    Ok(())
                });
                tagged.add(1, payload);
            }
            if self.replicas.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.replicas).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.replicas).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.replicas {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(2, payload);
            }
            if self.removing_replicas.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.removing_replicas).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.removing_replicas).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.removing_replicas {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(3, payload);
            }
            if self.adding_replicas.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.adding_replicas).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.adding_replicas).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.adding_replicas {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(4, payload);
            }
            if self.leader_recovery_state != -1i8 {
                let payload = encode_to_bytes(1, |b| {
                    put_i8(b, self.leader_recovery_state);
                    Ok(())
                });
                tagged.add(5, payload);
            }
            if self.directories.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.directories).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 16).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.directories).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.directories {
                                for it in v {
                                    crate::primitives::uuid::put_uuid(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(8, payload);
            }
            if self.eligible_leader_replicas.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.eligible_leader_replicas).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.eligible_leader_replicas).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.eligible_leader_replicas {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(6, payload);
            }
            if self.last_known_elr.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.last_known_elr).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.last_known_elr).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.last_known_elr {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(7, payload);
            }
            tagged.write(buf, &self.unknown_tagged_fields);
        }
    }
    fn decode_field_0<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.partition_id = get_i32(buf)?;
        }
        Ok(())
    }
    fn decode_field_1<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.topic_id = crate::primitives::uuid::get_uuid(buf)?;
        }
        Ok(())
    }
    fn decode_tagged_fields<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        _version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if flex {
            let mut tag_isr = None;
            let mut tag_leader = None;
            let mut tag_replicas = None;
            let mut tag_removing_replicas = None;
            let mut tag_adding_replicas = None;
            let mut tag_leader_recovery_state = None;
            let mut tag_directories = None;
            let mut tag_eligible_leader_replicas = None;
            let mut tag_last_known_elr = None;
            out.unknown_tagged_fields = read_tagged_fields(buf, |tag, payload| match tag {
                0 => {
                    tag_isr = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                1 => {
                    tag_leader = Some({
                        let b: &mut &[u8] = payload;
                        get_i32(b)?
                    });
                    Ok(true)
                }
                2 => {
                    tag_replicas = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                3 => {
                    tag_removing_replicas = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                4 => {
                    tag_adding_replicas = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                5 => {
                    tag_leader_recovery_state = Some({
                        let b: &mut &[u8] = payload;
                        get_i8(b)?
                    });
                    Ok(true)
                }
                8 => {
                    tag_directories = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(crate::primitives::uuid::get_uuid(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                6 => {
                    tag_eligible_leader_replicas = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                7 => {
                    tag_last_known_elr = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                _ => Ok(false),
            })?;
            if let Some(v) = tag_isr {
                out.isr = v;
            }
            if let Some(v) = tag_leader {
                out.leader = v;
            }
            if let Some(v) = tag_replicas {
                out.replicas = v;
            }
            if let Some(v) = tag_removing_replicas {
                out.removing_replicas = v;
            }
            if let Some(v) = tag_adding_replicas {
                out.adding_replicas = v;
            }
            if let Some(v) = tag_leader_recovery_state {
                out.leader_recovery_state = v;
            }
            if let Some(v) = tag_directories {
                out.directories = v;
            }
            if let Some(v) = tag_eligible_leader_replicas {
                out.eligible_leader_replicas = v;
            }
            if let Some(v) = tag_last_known_elr {
                out.last_known_elr = v;
            }
        }
        Ok(())
    }
}
impl Encode for PartitionChangeRecord {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::SchemaMismatch(
                "PartitionChangeRecord version out of range",
            ));
        }
        let flex = is_flexible(version);
        self.encode_field_0(buf, version, flex);
        self.encode_field_1(buf, version, flex);
        self.encode_tagged_fields(buf, version, flex);
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 16;
        }
        if flex {
            let mut known_pairs: Vec<(u32, usize)> = Vec::new();
            if self.isr.is_some() {
                known_pairs.push((0, {
                    let opt: Option<&Vec<_>> = (self.isr).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            if self.leader != -2i32 {
                known_pairs.push((1, 4));
            }
            if self.replicas.is_some() {
                known_pairs.push((2, {
                    let opt: Option<&Vec<_>> = (self.replicas).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            if self.removing_replicas.is_some() {
                known_pairs.push((3, {
                    let opt: Option<&Vec<_>> = (self.removing_replicas).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            if self.adding_replicas.is_some() {
                known_pairs.push((4, {
                    let opt: Option<&Vec<_>> = (self.adding_replicas).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            if self.leader_recovery_state != -1i8 {
                known_pairs.push((5, 1));
            }
            if self.directories.is_some() {
                known_pairs.push((8, {
                    let opt: Option<&Vec<_>> = (self.directories).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 16).sum());
                    prefix + body
                }));
            }
            if self.eligible_leader_replicas.is_some() {
                known_pairs.push((6, {
                    let opt: Option<&Vec<_>> = (self.eligible_leader_replicas).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            if self.last_known_elr.is_some() {
                known_pairs.push((7, {
                    let opt: Option<&Vec<_>> = (self.last_known_elr).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for PartitionChangeRecord {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::SchemaMismatch(
                "PartitionChangeRecord version out of range",
            ));
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        Self::decode_field_0(&mut out, buf, version, flex)?;
        Self::decode_field_1(&mut out, buf, version, flex)?;
        Self::decode_tagged_fields(&mut out, buf, version, flex)?;
        Ok(out)
    }
}
#[cfg(test)]
impl PartitionChangeRecord {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.partition_id = 1i32;
        }
        if version >= 0 {
            m.topic_id = crate::primitives::uuid::Uuid([1u8; 16]);
        }
        if version >= 0 {
            m.isr = Some(vec![1i32]);
        }
        if version >= 0 {
            m.leader = 1i32;
        }
        if version >= 0 {
            m.replicas = Some(vec![1i32]);
        }
        if version >= 0 {
            m.removing_replicas = Some(vec![1i32]);
        }
        if version >= 0 {
            m.adding_replicas = Some(vec![1i32]);
        }
        if version >= 0 {
            m.leader_recovery_state = 1i8;
        }
        if version >= 1 {
            m.directories = Some(vec![crate::primitives::uuid::Uuid([1u8; 16])]);
        }
        if version >= 2 {
            m.eligible_leader_replicas = Some(vec![1i32]);
        }
        if version >= 2 {
            m.last_known_elr = Some(vec![1i32]);
        }
        m
    }
}
/// Default JSON payload matching `Self::default()` for JVM oracle differential testing.
/// Only includes fields valid for the given version.
#[must_use]
#[allow(unused_comparisons)]
pub fn default_json(version: i16) -> ::serde_json::Value {
    let mut obj = ::serde_json::Map::new();
    obj.insert("partitionId".to_string(), ::serde_json::json!(-1));
    obj.insert(
        "topicId".to_string(),
        ::serde_json::Value::String("AAAAAAAAAAAAAAAAAAAAAA".to_string()),
    );
    obj.insert("isr".to_string(), ::serde_json::Value::Null);
    obj.insert("leader".to_string(), ::serde_json::json!(-2));
    obj.insert("replicas".to_string(), ::serde_json::Value::Null);
    obj.insert("removingReplicas".to_string(), ::serde_json::Value::Null);
    obj.insert("addingReplicas".to_string(), ::serde_json::Value::Null);
    obj.insert("leaderRecoveryState".to_string(), ::serde_json::json!(-1));
    if version >= 1 {
        obj.insert("directories".to_string(), ::serde_json::Value::Null);
    }
    if version >= 2 {
        obj.insert(
            "eligibleLeaderReplicas".to_string(),
            ::serde_json::Value::Null,
        );
    }
    if version >= 2 {
        obj.insert("lastKnownElr".to_string(), ::serde_json::Value::Null);
    }
    ::serde_json::Value::Object(obj)
}
