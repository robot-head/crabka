// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use crate::primitives::fixed::{
    get_i16, get_i32, get_i64, get_u16, put_i16, put_i32, put_i64, put_u16,
};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, get_compact_nullable_string_owned,
    get_compact_string_owned, get_nullable_string_owned, get_string_owned, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string, string_len,
};
use crate::tagged_fields::{WriteTaggedFields, read_tagged_fields, tagged_fields_len};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};
use bytes::{Buf, BufMut};
pub const API_KEY: i16 = 55;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 2;
pub const FLEXIBLE_MIN: i16 = 0;
#[inline]
fn is_flexible(version: i16) -> bool {
    version >= FLEXIBLE_MIN
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct DescribeQuorumResponse {
    pub error_code: i16,
    pub error_message: Option<String>,
    pub topics: Vec<TopicData>,
    pub nodes: Vec<Node>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for DescribeQuorumResponse {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion {
                api_key: API_KEY,
                version,
            });
        }
        let flex = is_flexible(version);
        if version >= 0 {
            put_i16(buf, self.error_code);
        }
        if version >= 2 {
            if flex {
                put_compact_nullable_string(buf, self.error_message.as_deref());
            } else {
                put_nullable_string(buf, self.error_message.as_deref());
            }
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.topics).len(), flex);
                for it in &self.topics {
                    it.encode(buf, version)?;
                }
            }
        }
        if version >= 2 {
            {
                crate::primitives::array::put_array_len(buf, (self.nodes).len(), flex);
                for it in &self.nodes {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 {
            n += 2;
        }
        if version >= 2 {
            n += if flex {
                compact_nullable_string_len(self.error_message.as_deref())
            } else {
                nullable_string_len(self.error_message.as_deref())
            };
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.topics).len(), flex);
                let body: usize = (self.topics).iter().map(|it| it.encoded_len(version)).sum();
                prefix + body
            };
        }
        if version >= 2 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.nodes).len(), flex);
                let body: usize = (self.nodes).iter().map(|it| it.encoded_len(version)).sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for DescribeQuorumResponse {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion {
                api_key: API_KEY,
                version,
            });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 {
            out.error_code = get_i16(buf)?;
        }
        if version >= 2 {
            out.error_message = if flex {
                get_compact_nullable_string_owned(buf)?
            } else {
                get_nullable_string_owned(buf)?
            };
        }
        if version >= 0 {
            out.topics = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(TopicData::decode(buf, version)?);
                }
                v
            };
        }
        if version >= 2 {
            out.nodes = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(Node::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl DescribeQuorumResponse {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.error_code = 1i16;
        }
        if version >= 2 {
            m.error_message = Some("x".to_string());
        }
        if version >= 0 {
            m.topics = vec![TopicData::populated(version)];
        }
        if version >= 2 {
            m.nodes = vec![Node::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct TopicData {
    pub topic_name: String,
    pub partitions: Vec<PartitionData>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for TopicData {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 {
            if flex {
                put_compact_string(buf, &self.topic_name);
            } else {
                put_string(buf, &self.topic_name);
            }
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.partitions).len(), flex);
                for it in &self.partitions {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 {
            n += if flex {
                compact_string_len(&self.topic_name)
            } else {
                string_len(&self.topic_name)
            };
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.partitions).len(), flex);
                let body: usize = (self.partitions)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for TopicData {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 {
            out.topic_name = if flex {
                get_compact_string_owned(buf)?
            } else {
                get_string_owned(buf)?
            };
        }
        if version >= 0 {
            out.partitions = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(PartitionData::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl TopicData {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.topic_name = "x".to_string();
        }
        if version >= 0 {
            m.partitions = vec![PartitionData::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct PartitionData {
    pub partition_index: i32,
    pub error_code: i16,
    pub error_message: Option<String>,
    pub leader_id: i32,
    pub leader_epoch: i32,
    pub high_watermark: i64,
    pub current_voters: Vec<super::common::describe_quorum_response::replica_state::ReplicaState>,
    pub observers: Vec<super::common::describe_quorum_response::replica_state::ReplicaState>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for PartitionData {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 {
            put_i32(buf, self.partition_index);
        }
        if version >= 0 {
            put_i16(buf, self.error_code);
        }
        if version >= 2 {
            if flex {
                put_compact_nullable_string(buf, self.error_message.as_deref());
            } else {
                put_nullable_string(buf, self.error_message.as_deref());
            }
        }
        if version >= 0 {
            put_i32(buf, self.leader_id);
        }
        if version >= 0 {
            put_i32(buf, self.leader_epoch);
        }
        if version >= 0 {
            put_i64(buf, self.high_watermark);
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.current_voters).len(), flex);
                for it in &self.current_voters {
                    it.encode(buf, version)?;
                }
            }
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.observers).len(), flex);
                for it in &self.observers {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 2;
        }
        if version >= 2 {
            n += if flex {
                compact_nullable_string_len(self.error_message.as_deref())
            } else {
                nullable_string_len(self.error_message.as_deref())
            };
        }
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 8;
        }
        if version >= 0 {
            n += {
                let prefix = crate::primitives::array::array_len_prefix_len(
                    (self.current_voters).len(),
                    flex,
                );
                let body: usize = (self.current_voters)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.observers).len(), flex);
                let body: usize = (self.observers)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for PartitionData {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 {
            out.partition_index = get_i32(buf)?;
        }
        if version >= 0 {
            out.error_code = get_i16(buf)?;
        }
        if version >= 2 {
            out.error_message = if flex {
                get_compact_nullable_string_owned(buf)?
            } else {
                get_nullable_string_owned(buf)?
            };
        }
        if version >= 0 {
            out.leader_id = get_i32(buf)?;
        }
        if version >= 0 {
            out.leader_epoch = get_i32(buf)?;
        }
        if version >= 0 {
            out.high_watermark = get_i64(buf)?;
        }
        if version >= 0 {
            out.current_voters = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(
                        super::common::describe_quorum_response::replica_state::ReplicaState::decode(
                            buf,
                            version,
                        )?,
                    );
                }
                v
            };
        }
        if version >= 0 {
            out.observers = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(
                        super::common::describe_quorum_response::replica_state::ReplicaState::decode(
                            buf,
                            version,
                        )?,
                    );
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl PartitionData {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.partition_index = 1i32;
        }
        if version >= 0 {
            m.error_code = 1i16;
        }
        if version >= 2 {
            m.error_message = Some("x".to_string());
        }
        if version >= 0 {
            m.leader_id = 1i32;
        }
        if version >= 0 {
            m.leader_epoch = 1i32;
        }
        if version >= 0 {
            m.high_watermark = 1i64;
        }
        if version >= 0 {
            m.current_voters = vec![
                super::common::describe_quorum_response::replica_state::ReplicaState::populated(
                    version,
                ),
            ];
        }
        if version >= 0 {
            m.observers = vec![
                super::common::describe_quorum_response::replica_state::ReplicaState::populated(
                    version,
                ),
            ];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct Node {
    pub node_id: i32,
    pub listeners: Vec<Listener>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for Node {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 2 {
            put_i32(buf, self.node_id);
        }
        if version >= 2 {
            {
                crate::primitives::array::put_array_len(buf, (self.listeners).len(), flex);
                for it in &self.listeners {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 2 {
            n += 4;
        }
        if version >= 2 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.listeners).len(), flex);
                let body: usize = (self.listeners)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for Node {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 2 {
            out.node_id = get_i32(buf)?;
        }
        if version >= 2 {
            out.listeners = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(Listener::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl Node {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 2 {
            m.node_id = 1i32;
        }
        if version >= 2 {
            m.listeners = vec![Listener::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct Listener {
    pub name: String,
    pub host: String,
    pub port: u16,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for Listener {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 2 {
            if flex {
                put_compact_string(buf, &self.name);
            } else {
                put_string(buf, &self.name);
            }
        }
        if version >= 2 {
            if flex {
                put_compact_string(buf, &self.host);
            } else {
                put_string(buf, &self.host);
            }
        }
        if version >= 2 {
            put_u16(buf, self.port);
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 2 {
            n += if flex {
                compact_string_len(&self.name)
            } else {
                string_len(&self.name)
            };
        }
        if version >= 2 {
            n += if flex {
                compact_string_len(&self.host)
            } else {
                string_len(&self.host)
            };
        }
        if version >= 2 {
            n += 2;
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for Listener {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 2 {
            out.name = if flex {
                get_compact_string_owned(buf)?
            } else {
                get_string_owned(buf)?
            };
        }
        if version >= 2 {
            out.host = if flex {
                get_compact_string_owned(buf)?
            } else {
                get_string_owned(buf)?
            };
        }
        if version >= 2 {
            out.port = get_u16(buf)?;
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl Listener {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 2 {
            m.name = "x".to_string();
        }
        if version >= 2 {
            m.host = "x".to_string();
        }
        if version >= 2 {
            m.port = 1u16;
        }
        m
    }
}
/// Default JSON payload matching `Self::default()` for JVM oracle differential testing.
/// Only includes fields valid for the given version.
#[must_use]
#[allow(unused_comparisons)]
pub fn default_json(version: i16) -> ::serde_json::Value {
    let mut obj = ::serde_json::Map::new();
    obj.insert("errorCode".to_string(), ::serde_json::json!(0));
    if version >= 2 {
        obj.insert("errorMessage".to_string(), ::serde_json::Value::Null);
    }
    obj.insert("topics".to_string(), ::serde_json::Value::Array(vec![]));
    if version >= 2 {
        obj.insert("nodes".to_string(), ::serde_json::Value::Array(vec![]));
    }
    ::serde_json::Value::Object(obj)
}
