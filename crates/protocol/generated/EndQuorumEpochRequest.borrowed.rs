// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use bytes::BufMut;

use crate::primitives::fixed::{get_i32, get_u16, put_i32, put_u16};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string,
    string_len,
};
use crate::primitives::string_bytes_borrowed::{
    get_compact_nullable_string_borrowed, get_compact_string_borrowed,
    get_nullable_string_borrowed, get_string_borrowed,
};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{DecodeBorrow, Encode, ProtocolError, UnknownTaggedFields};

pub const API_KEY: i16 = 54;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 1;
pub const FLEXIBLE_MIN: i16 = 1;

#[inline]
fn is_flexible(version: i16) -> bool { version >= FLEXIBLE_MIN }

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct EndQuorumEpochRequest<'a> {
    pub cluster_id: Option<&'a str>,
    pub topics: Vec<TopicData<'a>>,
    pub leader_endpoints: Vec<LeaderEndpoint<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for EndQuorumEpochRequest<'a> {
    fn default() -> Self {
        Self {
            cluster_id: None,
            topics: Vec::new(),
            leader_endpoints: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> EndQuorumEpochRequest<'a> {
    pub fn to_owned(&self) -> crate::owned::end_quorum_epoch_request::EndQuorumEpochRequest {
        crate::owned::end_quorum_epoch_request::EndQuorumEpochRequest {
            cluster_id: (self.cluster_id).map(|s| s.to_string()),
            topics: (self.topics).iter().map(|it| it.to_owned()).collect(),
            leader_endpoints: (self.leader_endpoints).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for EndQuorumEpochRequest<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        if version >= 0 { if flex { put_compact_nullable_string(buf, self.cluster_id) } else { put_nullable_string(buf, self.cluster_id) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.topics).len(), flex); for it in &self.topics { it.encode(buf, version)?; } } }
        if version >= 1 { { crate::primitives::array::put_array_len(buf, (self.leader_endpoints).len(), flex); for it in &self.leader_endpoints { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_nullable_string_len(self.cluster_id) } else { nullable_string_len(self.cluster_id) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.topics).len(), flex); let body: usize = (self.topics).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 1 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.leader_endpoints).len(), flex); let body: usize = (self.leader_endpoints).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for EndQuorumEpochRequest<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 { out.cluster_id = if flex { get_compact_nullable_string_borrowed(buf)? } else { get_nullable_string_borrowed(buf)? }; }
        if version >= 0 { out.topics = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(TopicData::decode_borrow(buf, version)?); } v }; }
        if version >= 1 { out.leader_endpoints = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(LeaderEndpoint::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct TopicData<'a> {
    pub topic_name: &'a str,
    pub partitions: Vec<PartitionData>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for TopicData<'a> {
    fn default() -> Self {
        Self {
            topic_name: "",
            partitions: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> TopicData<'a> {
    pub fn to_owned(&self) -> crate::owned::end_quorum_epoch_request::TopicData {
        crate::owned::end_quorum_epoch_request::TopicData {
            topic_name: (self.topic_name).to_string(),
            partitions: (self.partitions).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for TopicData<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 1;
        if version >= 0 { if flex { put_compact_string(buf, self.topic_name) } else { put_string(buf, self.topic_name) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.partitions).len(), flex); for it in &self.partitions { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 1;
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(self.topic_name) } else { string_len(self.topic_name) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.partitions).len(), flex); let body: usize = (self.partitions).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for TopicData<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 1;
        let mut out = Self::default();
        if version >= 0 { out.topic_name = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 0 { out.partitions = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(PartitionData::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PartitionData {
    pub partition_index: i32,
    pub leader_id: i32,
    pub leader_epoch: i32,
    pub preferred_successors: Vec<i32>,
    pub preferred_candidates: Vec<ReplicaInfo>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Default for PartitionData {
    fn default() -> Self {
        Self {
            partition_index: 0i32,
            leader_id: 0i32,
            leader_epoch: 0i32,
            preferred_successors: Vec::new(),
            preferred_candidates: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl PartitionData {
    pub fn to_owned(&self) -> crate::owned::end_quorum_epoch_request::PartitionData {
        crate::owned::end_quorum_epoch_request::PartitionData {
            partition_index: (self.partition_index),
            leader_id: (self.leader_id),
            leader_epoch: (self.leader_epoch),
            preferred_successors: (self.preferred_successors).clone(),
            preferred_candidates: (self.preferred_candidates).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl Encode for PartitionData {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 1;
        if version >= 0 { put_i32(buf, self.partition_index) }
        if version >= 0 { put_i32(buf, self.leader_id) }
        if version >= 0 { put_i32(buf, self.leader_epoch) }
        if version >= 0 && version <= 0 { { crate::primitives::array::put_array_len(buf, (self.preferred_successors).len(), flex); for it in &self.preferred_successors { put_i32(buf, *it); } } }
        if version >= 1 { { crate::primitives::array::put_array_len(buf, (self.preferred_candidates).len(), flex); for it in &self.preferred_candidates { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 1;
        let mut n: usize = 0;
        if version >= 0 { n += 4; }
        if version >= 0 { n += 4; }
        if version >= 0 { n += 4; }
        if version >= 0 && version <= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.preferred_successors).len(), flex); let body: usize = (self.preferred_successors).iter().map(|_| 4).sum(); prefix + body }; }
        if version >= 1 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.preferred_candidates).len(), flex); let body: usize = (self.preferred_candidates).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for PartitionData {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 1;
        let mut out = Self::default();
        if version >= 0 { out.partition_index = get_i32(buf)?; }
        if version >= 0 { out.leader_id = get_i32(buf)?; }
        if version >= 0 { out.leader_epoch = get_i32(buf)?; }
        if version >= 0 && version <= 0 { out.preferred_successors = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(get_i32(buf)?); } v }; }
        if version >= 1 { out.preferred_candidates = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(ReplicaInfo::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ReplicaInfo {
    pub candidate_id: i32,
    pub candidate_directory_id: crate::primitives::uuid::Uuid,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Default for ReplicaInfo {
    fn default() -> Self {
        Self {
            candidate_id: 0i32,
            candidate_directory_id: Default::default(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl ReplicaInfo {
    pub fn to_owned(&self) -> crate::owned::end_quorum_epoch_request::ReplicaInfo {
        crate::owned::end_quorum_epoch_request::ReplicaInfo {
            candidate_id: (self.candidate_id),
            candidate_directory_id: (self.candidate_directory_id),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl Encode for ReplicaInfo {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 1;
        if version >= 1 { put_i32(buf, self.candidate_id) }
        if version >= 1 { crate::primitives::uuid::put_uuid(buf, self.candidate_directory_id) }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 1;
        let mut n: usize = 0;
        if version >= 1 { n += 4; }
        if version >= 1 { n += 16; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for ReplicaInfo {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 1;
        let mut out = Self::default();
        if version >= 1 { out.candidate_id = get_i32(buf)?; }
        if version >= 1 { out.candidate_directory_id = crate::primitives::uuid::get_uuid(buf)?; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct LeaderEndpoint<'a> {
    pub name: &'a str,
    pub host: &'a str,
    pub port: u16,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for LeaderEndpoint<'a> {
    fn default() -> Self {
        Self {
            name: "",
            host: "",
            port: 0u16,
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> LeaderEndpoint<'a> {
    pub fn to_owned(&self) -> crate::owned::end_quorum_epoch_request::LeaderEndpoint {
        crate::owned::end_quorum_epoch_request::LeaderEndpoint {
            name: (self.name).to_string(),
            host: (self.host).to_string(),
            port: (self.port),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for LeaderEndpoint<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 1;
        if version >= 1 { if flex { put_compact_string(buf, self.name) } else { put_string(buf, self.name) } }
        if version >= 1 { if flex { put_compact_string(buf, self.host) } else { put_string(buf, self.host) } }
        if version >= 1 { put_u16(buf, self.port) }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 1;
        let mut n: usize = 0;
        if version >= 1 { n += if flex { compact_string_len(self.name) } else { string_len(self.name) }; }
        if version >= 1 { n += if flex { compact_string_len(self.host) } else { string_len(self.host) }; }
        if version >= 1 { n += 2; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for LeaderEndpoint<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 1;
        let mut out = Self::default();
        if version >= 1 { out.name = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 1 { out.host = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 1 { out.port = get_u16(buf)?; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}
