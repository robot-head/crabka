// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use bytes::{Buf, BufMut};

use crate::primitives::fixed::{get_i64, put_i64};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, get_compact_nullable_string_owned,
    get_compact_string_owned, get_nullable_string_owned, get_string_owned, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string,
    string_len,
};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};

pub const API_KEY: i16 = 66;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 2;
pub const FLEXIBLE_MIN: i16 = 0;

#[inline]
fn is_flexible(version: i16) -> bool { version >= FLEXIBLE_MIN }

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ListTransactionsRequest {
    pub state_filters: Vec<String>,
    pub producer_id_filters: Vec<i64>,
    pub duration_filter: i64,
    pub transactional_id_pattern: Option<String>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Default for ListTransactionsRequest {
    fn default() -> Self {
        Self {
            state_filters: Vec::new(),
            producer_id_filters: Vec::new(),
            duration_filter: -1i64,
            transactional_id_pattern: None,
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl Encode for ListTransactionsRequest {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.state_filters).len(), flex); for it in &self.state_filters { if flex { put_compact_string(buf, &*it) } else { put_string(buf, &*it) }; } } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.producer_id_filters).len(), flex); for it in &self.producer_id_filters { put_i64(buf, *it); } } }
        if version >= 1 { put_i64(buf, self.duration_filter) }
        if version >= 2 { if flex { put_compact_nullable_string(buf, self.transactional_id_pattern.as_deref()) } else { put_nullable_string(buf, self.transactional_id_pattern.as_deref()) } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.state_filters).len(), flex); let body: usize = (self.state_filters).iter().map(|it| if flex { compact_string_len(&*it) } else { string_len(&*it) }).sum(); prefix + body }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.producer_id_filters).len(), flex); let body: usize = (self.producer_id_filters).iter().map(|_| 8).sum(); prefix + body }; }
        if version >= 1 { n += 8; }
        if version >= 2 { n += if flex { compact_nullable_string_len(self.transactional_id_pattern.as_deref()) } else { nullable_string_len(self.transactional_id_pattern.as_deref()) }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> Decode<'de> for ListTransactionsRequest {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 { out.state_filters = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(if flex { get_compact_string_owned(buf)? } else { get_string_owned(buf)? }); } v }; }
        if version >= 0 { out.producer_id_filters = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(get_i64(buf)?); } v }; }
        if version >= 1 { out.duration_filter = get_i64(buf)?; }
        if version >= 2 { out.transactional_id_pattern = if flex { get_compact_nullable_string_owned(buf)? } else { get_nullable_string_owned(buf)? }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

/// Default JSON payload matching `Self::default()` for JVM oracle differential testing.
/// Only includes fields valid for the given version.
#[must_use]
#[allow(unused_comparisons)]
pub fn default_json(version: i16) -> ::serde_json::Value {
    let mut obj = ::serde_json::Map::new();
    obj.insert("stateFilters".to_string(), ::serde_json::Value::Array(vec![]));
    obj.insert("producerIdFilters".to_string(), ::serde_json::Value::Array(vec![]));
    if version >= 1 {
        obj.insert("durationFilter".to_string(), ::serde_json::json!(-1));
    }
    if version >= 2 {
        obj.insert("transactionalIdPattern".to_string(), ::serde_json::Value::Null);
    }
    ::serde_json::Value::Object(obj)
}

impl crate::ProtocolRequest for ListTransactionsRequest {
    const API_KEY: i16 = API_KEY;
    const MIN_VERSION: i16 = MIN_VERSION;
    const MAX_VERSION: i16 = MAX_VERSION;
    const FLEXIBLE_MIN: i16 = FLEXIBLE_MIN;
    type Response = super::list_transactions_response::ListTransactionsResponse;
}
