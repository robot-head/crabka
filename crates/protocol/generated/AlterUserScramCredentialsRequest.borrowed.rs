// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use bytes::{Bytes, BufMut};

use crate::primitives::fixed::{get_i32, get_i8, put_i32, put_i8};
use crate::primitives::string_bytes::{
    compact_string_len, put_compact_string, put_string, string_len,
};
use crate::primitives::string_bytes_borrowed::{
    get_compact_string_borrowed, get_string_borrowed,
};
use crate::primitives::string_bytes::{put_bytes, put_compact_bytes};
use crate::primitives::string_bytes_borrowed::{get_bytes_borrowed, get_compact_bytes_borrowed};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{DecodeBorrow, Encode, ProtocolError, UnknownTaggedFields};

pub const API_KEY: i16 = 51;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 0;
pub const FLEXIBLE_MIN: i16 = 0;

#[inline]
fn is_flexible(version: i16) -> bool { version >= FLEXIBLE_MIN }

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct AlterUserScramCredentialsRequest<'a> {
    pub deletions: Vec<ScramCredentialDeletion<'a>>,
    pub upsertions: Vec<ScramCredentialUpsertion<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for AlterUserScramCredentialsRequest<'a> {
    fn default() -> Self {
        Self {
            deletions: Vec::new(),
            upsertions: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> AlterUserScramCredentialsRequest<'a> {
    pub fn to_owned(&self) -> crate::owned::alter_user_scram_credentials_request::AlterUserScramCredentialsRequest {
        crate::owned::alter_user_scram_credentials_request::AlterUserScramCredentialsRequest {
            deletions: (self.deletions).iter().map(|it| it.to_owned()).collect(),
            upsertions: (self.upsertions).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for AlterUserScramCredentialsRequest<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.deletions).len(), flex); for it in &self.deletions { it.encode(buf, version)?; } } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.upsertions).len(), flex); for it in &self.upsertions { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.deletions).len(), flex); let body: usize = (self.deletions).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.upsertions).len(), flex); let body: usize = (self.upsertions).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for AlterUserScramCredentialsRequest<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 { out.deletions = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(ScramCredentialDeletion::decode_borrow(buf, version)?); } v }; }
        if version >= 0 { out.upsertions = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(ScramCredentialUpsertion::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ScramCredentialDeletion<'a> {
    pub name: &'a str,
    pub mechanism: i8,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for ScramCredentialDeletion<'a> {
    fn default() -> Self {
        Self {
            name: "",
            mechanism: 0i8,
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> ScramCredentialDeletion<'a> {
    pub fn to_owned(&self) -> crate::owned::alter_user_scram_credentials_request::ScramCredentialDeletion {
        crate::owned::alter_user_scram_credentials_request::ScramCredentialDeletion {
            name: (self.name).to_string(),
            mechanism: (self.mechanism),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for ScramCredentialDeletion<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { if flex { put_compact_string(buf, self.name) } else { put_string(buf, self.name) } }
        if version >= 0 { put_i8(buf, self.mechanism) }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(self.name) } else { string_len(self.name) }; }
        if version >= 0 { n += 1; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for ScramCredentialDeletion<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.name = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 0 { out.mechanism = get_i8(buf)?; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ScramCredentialUpsertion<'a> {
    pub name: &'a str,
    pub mechanism: i8,
    pub iterations: i32,
    pub salt: &'a [u8],
    pub salted_password: &'a [u8],
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for ScramCredentialUpsertion<'a> {
    fn default() -> Self {
        Self {
            name: "",
            mechanism: 0i8,
            iterations: 0i32,
            salt: &[],
            salted_password: &[],
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> ScramCredentialUpsertion<'a> {
    pub fn to_owned(&self) -> crate::owned::alter_user_scram_credentials_request::ScramCredentialUpsertion {
        crate::owned::alter_user_scram_credentials_request::ScramCredentialUpsertion {
            name: (self.name).to_string(),
            mechanism: (self.mechanism),
            iterations: (self.iterations),
            salt: Bytes::copy_from_slice(self.salt),
            salted_password: Bytes::copy_from_slice(self.salted_password),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for ScramCredentialUpsertion<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { if flex { put_compact_string(buf, self.name) } else { put_string(buf, self.name) } }
        if version >= 0 { put_i8(buf, self.mechanism) }
        if version >= 0 { put_i32(buf, self.iterations) }
        if version >= 0 { if flex { put_compact_bytes(buf, self.salt) } else { put_bytes(buf, self.salt) } }
        if version >= 0 { if flex { put_compact_bytes(buf, self.salted_password) } else { put_bytes(buf, self.salted_password) } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(self.name) } else { string_len(self.name) }; }
        if version >= 0 { n += 1; }
        if version >= 0 { n += 4; }
        if version >= 0 { n += if flex { crate::primitives::varint::uvarint_len(u32::try_from((self.salt).len() + 1).unwrap()) + (self.salt).len() } else { 4 + (self.salt).len() }; }
        if version >= 0 { n += if flex { crate::primitives::varint::uvarint_len(u32::try_from((self.salted_password).len() + 1).unwrap()) + (self.salted_password).len() } else { 4 + (self.salted_password).len() }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for ScramCredentialUpsertion<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.name = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 0 { out.mechanism = get_i8(buf)?; }
        if version >= 0 { out.iterations = get_i32(buf)?; }
        if version >= 0 { out.salt = if flex { get_compact_bytes_borrowed(buf)? } else { get_bytes_borrowed(buf)? }; }
        if version >= 0 { out.salted_password = if flex { get_compact_bytes_borrowed(buf)? } else { get_bytes_borrowed(buf)? }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}
