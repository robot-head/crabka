// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use crate::primitives::fixed::{get_i8, get_i32, put_i8, put_i32};
use crate::tagged_fields::{
    WriteTaggedFields, encode_to_bytes, read_tagged_fields, tagged_fields_len,
};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};
use bytes::{Buf, BufMut};
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 2;
pub const FLEXIBLE_MIN: i16 = 0;
#[inline]
#[must_use]
pub fn is_flexible(version: i16) -> bool {
    version >= FLEXIBLE_MIN
}
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PartitionRecord {
    pub partition_id: i32,
    pub topic_id: crate::primitives::uuid::Uuid,
    pub replicas: Vec<i32>,
    pub isr: Vec<i32>,
    pub removing_replicas: Vec<i32>,
    pub adding_replicas: Vec<i32>,
    pub leader: i32,
    pub leader_epoch: i32,
    pub partition_epoch: i32,
    pub directories: Vec<crate::primitives::uuid::Uuid>,
    pub leader_recovery_state: i8,
    pub eligible_leader_replicas: Option<Vec<i32>>,
    pub last_known_elr: Option<Vec<i32>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Default for PartitionRecord {
    fn default() -> Self {
        Self {
            partition_id: -1i32,
            topic_id: crate::primitives::uuid::Uuid::default(),
            replicas: Vec::new(),
            isr: Vec::new(),
            removing_replicas: Vec::new(),
            adding_replicas: Vec::new(),
            leader: -1i32,
            leader_epoch: -1i32,
            partition_epoch: -1i32,
            directories: Vec::new(),
            leader_recovery_state: 0i8,
            eligible_leader_replicas: None,
            last_known_elr: None,
            unknown_tagged_fields: UnknownTaggedFields::default(),
        }
    }
}
impl PartitionRecord {
    fn encode_field_0<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            put_i32(buf, self.partition_id);
        }
    }
    fn encode_field_1<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            crate::primitives::uuid::put_uuid(buf, self.topic_id);
        }
    }
    fn encode_field_2<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.replicas).len(), flex);
                for it in &self.replicas {
                    put_i32(buf, *it);
                }
            };
        }
    }
    fn encode_field_3<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.isr).len(), flex);
                for it in &self.isr {
                    put_i32(buf, *it);
                }
            };
        }
    }
    fn encode_field_4<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.removing_replicas).len(), flex);
                for it in &self.removing_replicas {
                    put_i32(buf, *it);
                }
            };
        }
    }
    fn encode_field_5<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.adding_replicas).len(), flex);
                for it in &self.adding_replicas {
                    put_i32(buf, *it);
                }
            };
        }
    }
    fn encode_field_6<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            put_i32(buf, self.leader);
        }
    }
    fn encode_field_7<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            put_i32(buf, self.leader_epoch);
        }
    }
    fn encode_field_8<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            put_i32(buf, self.partition_epoch);
        }
    }
    fn encode_field_9<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 1 {
            {
                crate::primitives::array::put_array_len(buf, (self.directories).len(), flex);
                for it in &self.directories {
                    crate::primitives::uuid::put_uuid(buf, *it);
                }
            };
        }
    }
    fn encode_tagged_fields<B: BufMut>(&self, buf: &mut B, _version: i16, flex: bool) {
        if flex {
            let mut tagged = WriteTaggedFields::new();
            if self.leader_recovery_state != 0i8 {
                let payload = encode_to_bytes(1, |b| {
                    put_i8(b, self.leader_recovery_state);
                    Ok(())
                });
                tagged.add(0, payload);
            }
            if self.eligible_leader_replicas.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.eligible_leader_replicas).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.eligible_leader_replicas).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.eligible_leader_replicas {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(1, payload);
            }
            if self.last_known_elr.is_some() {
                let payload = encode_to_bytes(
                    {
                        let opt: Option<&Vec<_>> = (self.last_known_elr).as_ref();
                        let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                            opt.map(std::vec::Vec::len),
                            flex,
                        );
                        let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                        prefix + body
                    },
                    |b| {
                        {
                            let len = (self.last_known_elr).as_ref().map(Vec::len);
                            crate::primitives::array::put_nullable_array_len(b, len, flex);
                            if let Some(v) = &self.last_known_elr {
                                for it in v {
                                    put_i32(b, *it);
                                }
                            }
                        };
                        Ok(())
                    },
                );
                tagged.add(2, payload);
            }
            tagged.write(buf, &self.unknown_tagged_fields);
        }
    }
    fn decode_field_0<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.partition_id = get_i32(buf)?;
        }
        Ok(())
    }
    fn decode_field_1<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.topic_id = crate::primitives::uuid::get_uuid(buf)?;
        }
        Ok(())
    }
    fn decode_field_2<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.replicas = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(get_i32(buf)?);
                }
                v
            };
        }
        Ok(())
    }
    fn decode_field_3<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.isr = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(get_i32(buf)?);
                }
                v
            };
        }
        Ok(())
    }
    fn decode_field_4<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.removing_replicas = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(get_i32(buf)?);
                }
                v
            };
        }
        Ok(())
    }
    fn decode_field_5<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.adding_replicas = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(get_i32(buf)?);
                }
                v
            };
        }
        Ok(())
    }
    fn decode_field_6<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.leader = get_i32(buf)?;
        }
        Ok(())
    }
    fn decode_field_7<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.leader_epoch = get_i32(buf)?;
        }
        Ok(())
    }
    fn decode_field_8<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.partition_epoch = get_i32(buf)?;
        }
        Ok(())
    }
    fn decode_field_9<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 1 {
            out.directories = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(crate::primitives::uuid::get_uuid(buf)?);
                }
                v
            };
        }
        Ok(())
    }
    fn decode_tagged_fields<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        _version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if flex {
            let mut tag_leader_recovery_state = None;
            let mut tag_eligible_leader_replicas = None;
            let mut tag_last_known_elr = None;
            out.unknown_tagged_fields = read_tagged_fields(buf, |tag, payload| match tag {
                0 => {
                    tag_leader_recovery_state = Some({
                        let b: &mut &[u8] = payload;
                        get_i8(b)?
                    });
                    Ok(true)
                }
                1 => {
                    tag_eligible_leader_replicas = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                2 => {
                    tag_last_known_elr = Some({
                        let b: &mut &[u8] = payload;
                        {
                            let opt = crate::primitives::array::get_nullable_array_len(b, flex)?;
                            match opt {
                                None => None,
                                Some(n) => {
                                    let mut v = Vec::with_capacity(n);
                                    for _ in 0..n {
                                        v.push(get_i32(b)?);
                                    }
                                    Some(v)
                                }
                            }
                        }
                    });
                    Ok(true)
                }
                _ => Ok(false),
            })?;
            if let Some(v) = tag_leader_recovery_state {
                out.leader_recovery_state = v;
            }
            if let Some(v) = tag_eligible_leader_replicas {
                out.eligible_leader_replicas = v;
            }
            if let Some(v) = tag_last_known_elr {
                out.last_known_elr = v;
            }
        }
        Ok(())
    }
}
impl Encode for PartitionRecord {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::SchemaMismatch(
                "PartitionRecord version out of range",
            ));
        }
        let flex = is_flexible(version);
        self.encode_field_0(buf, version, flex);
        self.encode_field_1(buf, version, flex);
        self.encode_field_2(buf, version, flex);
        self.encode_field_3(buf, version, flex);
        self.encode_field_4(buf, version, flex);
        self.encode_field_5(buf, version, flex);
        self.encode_field_6(buf, version, flex);
        self.encode_field_7(buf, version, flex);
        self.encode_field_8(buf, version, flex);
        self.encode_field_9(buf, version, flex);
        self.encode_tagged_fields(buf, version, flex);
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 16;
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.replicas).len(), flex);
                let body: usize = (self.replicas).iter().map(|_| 4).sum();
                prefix + body
            };
        }
        if version >= 0 {
            n += {
                let prefix = crate::primitives::array::array_len_prefix_len((self.isr).len(), flex);
                let body: usize = (self.isr).iter().map(|_| 4).sum();
                prefix + body
            };
        }
        if version >= 0 {
            n += {
                let prefix = crate::primitives::array::array_len_prefix_len(
                    (self.removing_replicas).len(),
                    flex,
                );
                let body: usize = (self.removing_replicas).iter().map(|_| 4).sum();
                prefix + body
            };
        }
        if version >= 0 {
            n += {
                let prefix = crate::primitives::array::array_len_prefix_len(
                    (self.adding_replicas).len(),
                    flex,
                );
                let body: usize = (self.adding_replicas).iter().map(|_| 4).sum();
                prefix + body
            };
        }
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 4;
        }
        if version >= 1 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.directories).len(), flex);
                let body: usize = (self.directories).iter().map(|_| 16).sum();
                prefix + body
            };
        }
        if flex {
            let mut known_pairs: Vec<(u32, usize)> = Vec::new();
            if self.leader_recovery_state != 0i8 {
                known_pairs.push((0, 1));
            }
            if self.eligible_leader_replicas.is_some() {
                known_pairs.push((1, {
                    let opt: Option<&Vec<_>> = (self.eligible_leader_replicas).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            if self.last_known_elr.is_some() {
                known_pairs.push((2, {
                    let opt: Option<&Vec<_>> = (self.last_known_elr).as_ref();
                    let prefix = crate::primitives::array::nullable_array_len_prefix_len(
                        opt.map(std::vec::Vec::len),
                        flex,
                    );
                    let body: usize = opt.map_or(0, |v| v.iter().map(|_| 4).sum());
                    prefix + body
                }));
            }
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for PartitionRecord {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::SchemaMismatch(
                "PartitionRecord version out of range",
            ));
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        Self::decode_field_0(&mut out, buf, version, flex)?;
        Self::decode_field_1(&mut out, buf, version, flex)?;
        Self::decode_field_2(&mut out, buf, version, flex)?;
        Self::decode_field_3(&mut out, buf, version, flex)?;
        Self::decode_field_4(&mut out, buf, version, flex)?;
        Self::decode_field_5(&mut out, buf, version, flex)?;
        Self::decode_field_6(&mut out, buf, version, flex)?;
        Self::decode_field_7(&mut out, buf, version, flex)?;
        Self::decode_field_8(&mut out, buf, version, flex)?;
        Self::decode_field_9(&mut out, buf, version, flex)?;
        Self::decode_tagged_fields(&mut out, buf, version, flex)?;
        Ok(out)
    }
}
#[cfg(test)]
impl PartitionRecord {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.partition_id = 1i32;
        }
        if version >= 0 {
            m.topic_id = crate::primitives::uuid::Uuid([1u8; 16]);
        }
        if version >= 0 {
            m.replicas = vec![1i32];
        }
        if version >= 0 {
            m.isr = vec![1i32];
        }
        if version >= 0 {
            m.removing_replicas = vec![1i32];
        }
        if version >= 0 {
            m.adding_replicas = vec![1i32];
        }
        if version >= 0 {
            m.leader = 1i32;
        }
        if version >= 0 {
            m.leader_epoch = 1i32;
        }
        if version >= 0 {
            m.partition_epoch = 1i32;
        }
        if version >= 1 {
            m.directories = vec![crate::primitives::uuid::Uuid([1u8; 16])];
        }
        if version >= 0 {
            m.leader_recovery_state = 1i8;
        }
        if version >= 2 {
            m.eligible_leader_replicas = Some(vec![1i32]);
        }
        if version >= 2 {
            m.last_known_elr = Some(vec![1i32]);
        }
        m
    }
}
/// Default JSON payload matching `Self::default()` for JVM oracle differential testing.
/// Only includes fields valid for the given version.
#[must_use]
#[allow(unused_comparisons)]
pub fn default_json(version: i16) -> ::serde_json::Value {
    let mut obj = ::serde_json::Map::new();
    obj.insert("partitionId".to_string(), ::serde_json::json!(-1));
    obj.insert(
        "topicId".to_string(),
        ::serde_json::Value::String("AAAAAAAAAAAAAAAAAAAAAA".to_string()),
    );
    obj.insert("replicas".to_string(), ::serde_json::Value::Array(vec![]));
    obj.insert("isr".to_string(), ::serde_json::Value::Array(vec![]));
    obj.insert(
        "removingReplicas".to_string(),
        ::serde_json::Value::Array(vec![]),
    );
    obj.insert(
        "addingReplicas".to_string(),
        ::serde_json::Value::Array(vec![]),
    );
    obj.insert("leader".to_string(), ::serde_json::json!(-1));
    obj.insert("leaderRecoveryState".to_string(), ::serde_json::json!(0));
    obj.insert("leaderEpoch".to_string(), ::serde_json::json!(-1));
    obj.insert("partitionEpoch".to_string(), ::serde_json::json!(-1));
    if version >= 1 {
        obj.insert(
            "directories".to_string(),
            ::serde_json::Value::Array(vec![]),
        );
    }
    if version >= 2 {
        obj.insert(
            "eligibleLeaderReplicas".to_string(),
            ::serde_json::Value::Null,
        );
    }
    if version >= 2 {
        obj.insert("lastKnownElr".to_string(), ::serde_json::Value::Null);
    }
    ::serde_json::Value::Object(obj)
}
