// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use bytes::BufMut;

use crate::primitives::fixed::{get_i16, get_i32, get_i64, put_i16, put_i32, put_i64};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string,
    string_len,
};
use crate::primitives::string_bytes_borrowed::{
    get_compact_nullable_string_borrowed, get_compact_string_borrowed,
    get_nullable_string_borrowed, get_string_borrowed,
};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{DecodeBorrow, Encode, ProtocolError, UnknownTaggedFields};

pub const API_KEY: i16 = 61;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 0;
pub const FLEXIBLE_MIN: i16 = 0;

#[inline]
fn is_flexible(version: i16) -> bool { version >= FLEXIBLE_MIN }

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct DescribeProducersResponse<'a> {
    pub throttle_time_ms: i32,
    pub topics: Vec<TopicResponse<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for DescribeProducersResponse<'a> {
    fn default() -> Self {
        Self {
            throttle_time_ms: 0i32,
            topics: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> DescribeProducersResponse<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_producers_response::DescribeProducersResponse {
        crate::owned::describe_producers_response::DescribeProducersResponse {
            throttle_time_ms: (self.throttle_time_ms),
            topics: (self.topics).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for DescribeProducersResponse<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        if version >= 0 { put_i32(buf, self.throttle_time_ms) }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.topics).len(), flex); for it in &self.topics { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 { n += 4; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.topics).len(), flex); let body: usize = (self.topics).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for DescribeProducersResponse<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 { out.throttle_time_ms = get_i32(buf)?; }
        if version >= 0 { out.topics = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(TopicResponse::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl<'a> DescribeProducersResponse<'a> {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.throttle_time_ms = 1i32; }
        if version >= 0 { m.topics = vec![TopicResponse::populated(version)]; }
        m
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct TopicResponse<'a> {
    pub name: &'a str,
    pub partitions: Vec<PartitionResponse<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for TopicResponse<'a> {
    fn default() -> Self {
        Self {
            name: "",
            partitions: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> TopicResponse<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_producers_response::TopicResponse {
        crate::owned::describe_producers_response::TopicResponse {
            name: (self.name).to_string(),
            partitions: (self.partitions).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for TopicResponse<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { if flex { put_compact_string(buf, self.name) } else { put_string(buf, self.name) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.partitions).len(), flex); for it in &self.partitions { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(self.name) } else { string_len(self.name) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.partitions).len(), flex); let body: usize = (self.partitions).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for TopicResponse<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.name = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 0 { out.partitions = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(PartitionResponse::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl<'a> TopicResponse<'a> {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.name = "x"; }
        if version >= 0 { m.partitions = vec![PartitionResponse::populated(version)]; }
        m
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PartitionResponse<'a> {
    pub partition_index: i32,
    pub error_code: i16,
    pub error_message: Option<&'a str>,
    pub active_producers: Vec<ProducerState>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for PartitionResponse<'a> {
    fn default() -> Self {
        Self {
            partition_index: 0i32,
            error_code: 0i16,
            error_message: None,
            active_producers: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> PartitionResponse<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_producers_response::PartitionResponse {
        crate::owned::describe_producers_response::PartitionResponse {
            partition_index: (self.partition_index),
            error_code: (self.error_code),
            error_message: (self.error_message).map(|s| s.to_string()),
            active_producers: (self.active_producers).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for PartitionResponse<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { put_i32(buf, self.partition_index) }
        if version >= 0 { put_i16(buf, self.error_code) }
        if version >= 0 { if flex { put_compact_nullable_string(buf, self.error_message) } else { put_nullable_string(buf, self.error_message) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.active_producers).len(), flex); for it in &self.active_producers { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += 4; }
        if version >= 0 { n += 2; }
        if version >= 0 { n += if flex { compact_nullable_string_len(self.error_message) } else { nullable_string_len(self.error_message) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.active_producers).len(), flex); let body: usize = (self.active_producers).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for PartitionResponse<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.partition_index = get_i32(buf)?; }
        if version >= 0 { out.error_code = get_i16(buf)?; }
        if version >= 0 { out.error_message = if flex { get_compact_nullable_string_borrowed(buf)? } else { get_nullable_string_borrowed(buf)? }; }
        if version >= 0 { out.active_producers = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(ProducerState::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl<'a> PartitionResponse<'a> {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.partition_index = 1i32; }
        if version >= 0 { m.error_code = 1i16; }
        if version >= 0 { m.error_message = Some("x"); }
        if version >= 0 { m.active_producers = vec![ProducerState::populated(version)]; }
        m
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ProducerState {
    pub producer_id: i64,
    pub producer_epoch: i32,
    pub last_sequence: i32,
    pub last_timestamp: i64,
    pub coordinator_epoch: i32,
    pub current_txn_start_offset: i64,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Default for ProducerState {
    fn default() -> Self {
        Self {
            producer_id: 0i64,
            producer_epoch: 0i32,
            last_sequence: -1i32,
            last_timestamp: -1i64,
            coordinator_epoch: 0i32,
            current_txn_start_offset: -1i64,
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl ProducerState {
    pub fn to_owned(&self) -> crate::owned::describe_producers_response::ProducerState {
        crate::owned::describe_producers_response::ProducerState {
            producer_id: (self.producer_id),
            producer_epoch: (self.producer_epoch),
            last_sequence: (self.last_sequence),
            last_timestamp: (self.last_timestamp),
            coordinator_epoch: (self.coordinator_epoch),
            current_txn_start_offset: (self.current_txn_start_offset),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl Encode for ProducerState {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { put_i64(buf, self.producer_id) }
        if version >= 0 { put_i32(buf, self.producer_epoch) }
        if version >= 0 { put_i32(buf, self.last_sequence) }
        if version >= 0 { put_i64(buf, self.last_timestamp) }
        if version >= 0 { put_i32(buf, self.coordinator_epoch) }
        if version >= 0 { put_i64(buf, self.current_txn_start_offset) }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += 8; }
        if version >= 0 { n += 4; }
        if version >= 0 { n += 4; }
        if version >= 0 { n += 8; }
        if version >= 0 { n += 4; }
        if version >= 0 { n += 8; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for ProducerState {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.producer_id = get_i64(buf)?; }
        if version >= 0 { out.producer_epoch = get_i32(buf)?; }
        if version >= 0 { out.last_sequence = get_i32(buf)?; }
        if version >= 0 { out.last_timestamp = get_i64(buf)?; }
        if version >= 0 { out.coordinator_epoch = get_i32(buf)?; }
        if version >= 0 { out.current_txn_start_offset = get_i64(buf)?; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl ProducerState {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.producer_id = 1i64; }
        if version >= 0 { m.producer_epoch = 1i32; }
        if version >= 0 { m.last_sequence = 1i32; }
        if version >= 0 { m.last_timestamp = 1i64; }
        if version >= 0 { m.coordinator_epoch = 1i32; }
        if version >= 0 { m.current_txn_start_offset = 1i64; }
        m
    }
}
