// AUTO-GENERATED by crabka-protocol-codegen against b25e89b6f842f11e4eb747e131a71153d9270536. Do not edit.

use bytes::{Buf, BufMut};

use crate::primitives::fixed::{get_i16, get_i32, get_i64, put_i16, put_i32, put_i64};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, get_compact_nullable_string_owned,
    get_compact_string_owned, get_nullable_string_owned, get_string_owned, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string,
    string_len,
};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};

pub const API_KEY: i16 = 0;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 9;
pub const FLEXIBLE_MIN: i16 = 9;

#[inline]
fn is_flexible(version: i16) -> bool { version >= FLEXIBLE_MIN }

#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct ProduceResponse {
    pub responses: Vec<TopicProduceResponse>,
    pub throttle_time_ms: i32,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Encode for ProduceResponse {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.responses).len(), flex); for it in &self.responses { it.encode(buf, version)?; } } }
        if version >= 1 { put_i32(buf, self.throttle_time_ms) }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.responses).len(), flex); let body: usize = (self.responses).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 1 { n += 4; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> Decode<'de> for ProduceResponse {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 { out.responses = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(TopicProduceResponse::decode(buf, version)?); } v }; }
        if version >= 1 { out.throttle_time_ms = get_i32(buf)?; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl ProduceResponse {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.responses = vec![TopicProduceResponse::populated(version)]; }
        if version >= 1 { m.throttle_time_ms = 1i32; }
        m
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct TopicProduceResponse {
    pub name: String,
    pub partition_responses: Vec<PartitionProduceResponse>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Encode for TopicProduceResponse {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 9;
        if version >= 0 { if flex { put_compact_string(buf, &self.name) } else { put_string(buf, &self.name) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.partition_responses).len(), flex); for it in &self.partition_responses { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 9;
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(&self.name) } else { string_len(&self.name) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.partition_responses).len(), flex); let body: usize = (self.partition_responses).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> Decode<'de> for TopicProduceResponse {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 9;
        let mut out = Self::default();
        if version >= 0 { out.name = if flex { get_compact_string_owned(buf)? } else { get_string_owned(buf)? }; }
        if version >= 0 { out.partition_responses = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(PartitionProduceResponse::decode(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl TopicProduceResponse {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.name = "x".to_string(); }
        if version >= 0 { m.partition_responses = vec![PartitionProduceResponse::populated(version)]; }
        m
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PartitionProduceResponse {
    pub index: i32,
    pub error_code: i16,
    pub base_offset: i64,
    pub log_append_time_ms: i64,
    pub log_start_offset: i64,
    pub record_errors: Vec<BatchIndexAndErrorMessage>,
    pub error_message: Option<String>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Default for PartitionProduceResponse {
    fn default() -> Self {
        Self {
            index: 0i32,
            error_code: 0i16,
            base_offset: 0i64,
            log_append_time_ms: -1i64,
            log_start_offset: -1i64,
            record_errors: Vec::new(),
            error_message: None,
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl Encode for PartitionProduceResponse {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 9;
        if version >= 0 { put_i32(buf, self.index) }
        if version >= 0 { put_i16(buf, self.error_code) }
        if version >= 0 { put_i64(buf, self.base_offset) }
        if version >= 2 { put_i64(buf, self.log_append_time_ms) }
        if version >= 5 { put_i64(buf, self.log_start_offset) }
        if version >= 8 { { crate::primitives::array::put_array_len(buf, (self.record_errors).len(), flex); for it in &self.record_errors { it.encode(buf, version)?; } } }
        if version >= 8 { if flex { put_compact_nullable_string(buf, self.error_message.as_deref()) } else { put_nullable_string(buf, self.error_message.as_deref()) } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 9;
        let mut n: usize = 0;
        if version >= 0 { n += 4; }
        if version >= 0 { n += 2; }
        if version >= 0 { n += 8; }
        if version >= 2 { n += 8; }
        if version >= 5 { n += 8; }
        if version >= 8 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.record_errors).len(), flex); let body: usize = (self.record_errors).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 8 { n += if flex { compact_nullable_string_len(self.error_message.as_deref()) } else { nullable_string_len(self.error_message.as_deref()) }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> Decode<'de> for PartitionProduceResponse {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 9;
        let mut out = Self::default();
        if version >= 0 { out.index = get_i32(buf)?; }
        if version >= 0 { out.error_code = get_i16(buf)?; }
        if version >= 0 { out.base_offset = get_i64(buf)?; }
        if version >= 2 { out.log_append_time_ms = get_i64(buf)?; }
        if version >= 5 { out.log_start_offset = get_i64(buf)?; }
        if version >= 8 { out.record_errors = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(BatchIndexAndErrorMessage::decode(buf, version)?); } v }; }
        if version >= 8 { out.error_message = if flex { get_compact_nullable_string_owned(buf)? } else { get_nullable_string_owned(buf)? }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl PartitionProduceResponse {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.index = 1i32; }
        if version >= 0 { m.error_code = 1i16; }
        if version >= 0 { m.base_offset = 1i64; }
        if version >= 2 { m.log_append_time_ms = 1i64; }
        if version >= 5 { m.log_start_offset = 1i64; }
        if version >= 8 { m.record_errors = vec![BatchIndexAndErrorMessage::populated(version)]; }
        if version >= 8 { m.error_message = Some("x".to_string()); }
        m
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct BatchIndexAndErrorMessage {
    pub batch_index: i32,
    pub batch_index_error_message: Option<String>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Encode for BatchIndexAndErrorMessage {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 9;
        if version >= 8 { put_i32(buf, self.batch_index) }
        if version >= 8 { if flex { put_compact_nullable_string(buf, self.batch_index_error_message.as_deref()) } else { put_nullable_string(buf, self.batch_index_error_message.as_deref()) } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 9;
        let mut n: usize = 0;
        if version >= 8 { n += 4; }
        if version >= 8 { n += if flex { compact_nullable_string_len(self.batch_index_error_message.as_deref()) } else { nullable_string_len(self.batch_index_error_message.as_deref()) }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> Decode<'de> for BatchIndexAndErrorMessage {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 9;
        let mut out = Self::default();
        if version >= 8 { out.batch_index = get_i32(buf)?; }
        if version >= 8 { out.batch_index_error_message = if flex { get_compact_nullable_string_owned(buf)? } else { get_nullable_string_owned(buf)? }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl BatchIndexAndErrorMessage {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 8 { m.batch_index = 1i32; }
        if version >= 8 { m.batch_index_error_message = Some("x".to_string()); }
        m
    }
}

/// Default JSON payload matching `Self::default()` for JVM oracle differential testing.
/// Only includes fields valid for the given version.
#[must_use]
#[allow(unused_comparisons)]
pub fn default_json(version: i16) -> ::serde_json::Value {
    let mut obj = ::serde_json::Map::new();
    obj.insert("responses".to_string(), ::serde_json::Value::Array(vec![]));
    if version >= 1 {
        obj.insert("throttleTimeMs".to_string(), ::serde_json::json!(0));
    }
    ::serde_json::Value::Object(obj)
}
