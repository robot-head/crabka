// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use crate::primitives::fixed::{
    get_bool, get_i8, get_i16, get_i32, put_bool, put_i8, put_i16, put_i32,
};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, get_compact_nullable_string_owned,
    get_compact_string_owned, get_nullable_string_owned, get_string_owned, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string, string_len,
};
use crate::tagged_fields::{WriteTaggedFields, read_tagged_fields, tagged_fields_len};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};
use bytes::{Buf, BufMut};
pub const API_KEY: i16 = 32;
pub const MIN_VERSION: i16 = 1;
pub const MAX_VERSION: i16 = 4;
pub const FLEXIBLE_MIN: i16 = 4;
#[inline]
#[must_use]
pub fn is_flexible(version: i16) -> bool {
    version >= FLEXIBLE_MIN
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct DescribeConfigsResponse {
    pub throttle_time_ms: i32,
    pub results: Vec<DescribeConfigsResult>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for DescribeConfigsResponse {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion {
                api_key: API_KEY,
                version,
            });
        }
        let flex = is_flexible(version);
        if version >= 0 {
            put_i32(buf, self.throttle_time_ms);
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.results).len(), flex);
                for it in &self.results {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.results).len(), flex);
                let body: usize = (self.results)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for DescribeConfigsResponse {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion {
                api_key: API_KEY,
                version,
            });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 {
            out.throttle_time_ms = get_i32(buf)?;
        }
        if version >= 0 {
            out.results = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(DescribeConfigsResult::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl DescribeConfigsResponse {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.throttle_time_ms = 1i32;
        }
        if version >= 0 {
            m.results = vec![DescribeConfigsResult::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct DescribeConfigsResult {
    pub error_code: i16,
    pub error_message: Option<String>,
    pub resource_type: i8,
    pub resource_name: String,
    pub configs: Vec<DescribeConfigsResourceResult>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for DescribeConfigsResult {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 4;
        if version >= 0 {
            put_i16(buf, self.error_code);
        }
        if version >= 0 {
            if flex {
                let () = put_compact_nullable_string(buf, self.error_message.as_deref());
            } else {
                let () = put_nullable_string(buf, self.error_message.as_deref());
            }
        }
        if version >= 0 {
            put_i8(buf, self.resource_type);
        }
        if version >= 0 {
            if flex {
                let () = put_compact_string(buf, &self.resource_name);
            } else {
                let () = put_string(buf, &self.resource_name);
            }
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.configs).len(), flex);
                for it in &self.configs {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 4;
        let mut n: usize = 0;
        if version >= 0 {
            n += 2;
        }
        if version >= 0 {
            n += if flex {
                compact_nullable_string_len(self.error_message.as_deref())
            } else {
                nullable_string_len(self.error_message.as_deref())
            };
        }
        if version >= 0 {
            n += 1;
        }
        if version >= 0 {
            n += if flex {
                compact_string_len(&self.resource_name)
            } else {
                string_len(&self.resource_name)
            };
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.configs).len(), flex);
                let body: usize = (self.configs)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for DescribeConfigsResult {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 4;
        let mut out = Self::default();
        if version >= 0 {
            out.error_code = get_i16(buf)?;
        }
        if version >= 0 {
            out.error_message = if flex {
                get_compact_nullable_string_owned(buf)?
            } else {
                get_nullable_string_owned(buf)?
            };
        }
        if version >= 0 {
            out.resource_type = get_i8(buf)?;
        }
        if version >= 0 {
            out.resource_name = if flex {
                get_compact_string_owned(buf)?
            } else {
                get_string_owned(buf)?
            };
        }
        if version >= 0 {
            out.configs = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(DescribeConfigsResourceResult::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl DescribeConfigsResult {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.error_code = 1i16;
        }
        if version >= 0 {
            m.error_message = Some("x".to_string());
        }
        if version >= 0 {
            m.resource_type = 1i8;
        }
        if version >= 0 {
            m.resource_name = "x".to_string();
        }
        if version >= 0 {
            m.configs = vec![DescribeConfigsResourceResult::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct DescribeConfigsResourceResult {
    pub name: String,
    pub value: Option<String>,
    pub read_only: bool,
    pub config_source: i8,
    pub is_sensitive: bool,
    pub synonyms: Vec<DescribeConfigsSynonym>,
    pub config_type: i8,
    pub documentation: Option<String>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Default for DescribeConfigsResourceResult {
    fn default() -> Self {
        Self {
            name: String::new(),
            value: None,
            read_only: false,
            config_source: -1i8,
            is_sensitive: false,
            synonyms: Vec::new(),
            config_type: 0i8,
            documentation: None,
            unknown_tagged_fields: UnknownTaggedFields::default(),
        }
    }
}
impl DescribeConfigsResourceResult {
    fn encode_field_0<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 0 {
            if flex {
                let () = put_compact_string(buf, &self.name);
            } else {
                let () = put_string(buf, &self.name);
            }
        }
    }
    fn encode_field_1<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 0 {
            if flex {
                let () = put_compact_nullable_string(buf, self.value.as_deref());
            } else {
                let () = put_nullable_string(buf, self.value.as_deref());
            }
        }
    }
    fn encode_field_2<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            put_bool(buf, self.read_only);
        }
    }
    fn encode_field_3<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 1 {
            put_i8(buf, self.config_source);
        }
    }
    fn encode_field_4<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 0 {
            put_bool(buf, self.is_sensitive);
        }
    }
    fn encode_field_5<B: BufMut>(
        &self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 1 {
            {
                crate::primitives::array::put_array_len(buf, (self.synonyms).len(), flex);
                for it in &self.synonyms {
                    it.encode(buf, version)?;
                }
            }
        }
        Ok(())
    }
    fn encode_field_6<B: BufMut>(&self, buf: &mut B, version: i16, _flex: bool) {
        if version >= 3 {
            put_i8(buf, self.config_type);
        }
    }
    fn encode_field_7<B: BufMut>(&self, buf: &mut B, version: i16, flex: bool) {
        if version >= 3 {
            if flex {
                let () = put_compact_nullable_string(buf, self.documentation.as_deref());
            } else {
                let () = put_nullable_string(buf, self.documentation.as_deref());
            }
        }
    }
    fn encode_tagged_fields<B: BufMut>(&self, buf: &mut B, _version: i16, flex: bool) {
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
    }
    fn decode_field_0<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.name = if flex {
                get_compact_string_owned(buf)?
            } else {
                get_string_owned(buf)?
            };
        }
        Ok(())
    }
    fn decode_field_1<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.value = if flex {
                get_compact_nullable_string_owned(buf)?
            } else {
                get_nullable_string_owned(buf)?
            };
        }
        Ok(())
    }
    fn decode_field_2<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.read_only = get_bool(buf)?;
        }
        Ok(())
    }
    fn decode_field_3<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 1 {
            out.config_source = get_i8(buf)?;
        }
        Ok(())
    }
    fn decode_field_4<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 0 {
            out.is_sensitive = get_bool(buf)?;
        }
        Ok(())
    }
    fn decode_field_5<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 1 {
            out.synonyms = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(DescribeConfigsSynonym::decode(buf, version)?);
                }
                v
            };
        }
        Ok(())
    }
    fn decode_field_6<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        _flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 3 {
            out.config_type = get_i8(buf)?;
        }
        Ok(())
    }
    fn decode_field_7<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if version >= 3 {
            out.documentation = if flex {
                get_compact_nullable_string_owned(buf)?
            } else {
                get_nullable_string_owned(buf)?
            };
        }
        Ok(())
    }
    fn decode_tagged_fields<B: Buf>(
        out: &mut Self,
        buf: &mut B,
        _version: i16,
        flex: bool,
    ) -> Result<(), ProtocolError> {
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(())
    }
}
impl Encode for DescribeConfigsResourceResult {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 4;
        self.encode_field_0(buf, version, flex);
        self.encode_field_1(buf, version, flex);
        self.encode_field_2(buf, version, flex);
        self.encode_field_3(buf, version, flex);
        self.encode_field_4(buf, version, flex);
        self.encode_field_5(buf, version, flex)?;
        self.encode_field_6(buf, version, flex);
        self.encode_field_7(buf, version, flex);
        self.encode_tagged_fields(buf, version, flex);
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 4;
        let mut n: usize = 0;
        if version >= 0 {
            n += if flex {
                compact_string_len(&self.name)
            } else {
                string_len(&self.name)
            };
        }
        if version >= 0 {
            n += if flex {
                compact_nullable_string_len(self.value.as_deref())
            } else {
                nullable_string_len(self.value.as_deref())
            };
        }
        if version >= 0 {
            n += 1;
        }
        if version >= 1 {
            n += 1;
        }
        if version >= 0 {
            n += 1;
        }
        if version >= 1 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.synonyms).len(), flex);
                let body: usize = (self.synonyms)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if version >= 3 {
            n += 1;
        }
        if version >= 3 {
            n += if flex {
                compact_nullable_string_len(self.documentation.as_deref())
            } else {
                nullable_string_len(self.documentation.as_deref())
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for DescribeConfigsResourceResult {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 4;
        let mut out = Self::default();
        Self::decode_field_0(&mut out, buf, version, flex)?;
        Self::decode_field_1(&mut out, buf, version, flex)?;
        Self::decode_field_2(&mut out, buf, version, flex)?;
        Self::decode_field_3(&mut out, buf, version, flex)?;
        Self::decode_field_4(&mut out, buf, version, flex)?;
        Self::decode_field_5(&mut out, buf, version, flex)?;
        Self::decode_field_6(&mut out, buf, version, flex)?;
        Self::decode_field_7(&mut out, buf, version, flex)?;
        Self::decode_tagged_fields(&mut out, buf, version, flex)?;
        Ok(out)
    }
}
#[cfg(test)]
impl DescribeConfigsResourceResult {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.name = "x".to_string();
        }
        if version >= 0 {
            m.value = Some("x".to_string());
        }
        if version >= 0 {
            m.read_only = true;
        }
        if version >= 1 {
            m.config_source = 1i8;
        }
        if version >= 0 {
            m.is_sensitive = true;
        }
        if version >= 1 {
            m.synonyms = vec![DescribeConfigsSynonym::populated(version)];
        }
        if version >= 3 {
            m.config_type = 1i8;
        }
        if version >= 3 {
            m.documentation = Some("x".to_string());
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct DescribeConfigsSynonym {
    pub name: String,
    pub value: Option<String>,
    pub source: i8,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for DescribeConfigsSynonym {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 4;
        if version >= 1 {
            if flex {
                let () = put_compact_string(buf, &self.name);
            } else {
                let () = put_string(buf, &self.name);
            }
        }
        if version >= 1 {
            if flex {
                let () = put_compact_nullable_string(buf, self.value.as_deref());
            } else {
                let () = put_nullable_string(buf, self.value.as_deref());
            }
        }
        if version >= 1 {
            put_i8(buf, self.source);
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 4;
        let mut n: usize = 0;
        if version >= 1 {
            n += if flex {
                compact_string_len(&self.name)
            } else {
                string_len(&self.name)
            };
        }
        if version >= 1 {
            n += if flex {
                compact_nullable_string_len(self.value.as_deref())
            } else {
                nullable_string_len(self.value.as_deref())
            };
        }
        if version >= 1 {
            n += 1;
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for DescribeConfigsSynonym {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 4;
        let mut out = Self::default();
        if version >= 1 {
            out.name = if flex {
                get_compact_string_owned(buf)?
            } else {
                get_string_owned(buf)?
            };
        }
        if version >= 1 {
            out.value = if flex {
                get_compact_nullable_string_owned(buf)?
            } else {
                get_nullable_string_owned(buf)?
            };
        }
        if version >= 1 {
            out.source = get_i8(buf)?;
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl DescribeConfigsSynonym {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 1 {
            m.name = "x".to_string();
        }
        if version >= 1 {
            m.value = Some("x".to_string());
        }
        if version >= 1 {
            m.source = 1i8;
        }
        m
    }
}
/// Default JSON payload matching `Self::default()` for JVM oracle differential testing.
/// Only includes fields valid for the given version.
#[must_use]
#[allow(unused_comparisons)]
pub fn default_json(_version: i16) -> ::serde_json::Value {
    let mut obj = ::serde_json::Map::new();
    obj.insert("throttleTimeMs".to_string(), ::serde_json::json!(0));
    obj.insert("results".to_string(), ::serde_json::Value::Array(vec![]));
    ::serde_json::Value::Object(obj)
}
