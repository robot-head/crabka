// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use bytes::BufMut;

use crate::primitives::fixed::{get_i16, get_i32, get_i64, get_u16, put_i16, put_i32, put_i64, put_u16};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string,
    string_len,
};
use crate::primitives::string_bytes_borrowed::{
    get_compact_nullable_string_borrowed, get_compact_string_borrowed,
    get_nullable_string_borrowed, get_string_borrowed,
};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{DecodeBorrow, Encode, ProtocolError, UnknownTaggedFields};

pub const API_KEY: i16 = 55;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 2;
pub const FLEXIBLE_MIN: i16 = 0;

#[inline]
fn is_flexible(version: i16) -> bool { version >= FLEXIBLE_MIN }

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct DescribeQuorumResponse<'a> {
    pub error_code: i16,
    pub error_message: Option<&'a str>,
    pub topics: Vec<TopicData<'a>>,
    pub nodes: Vec<Node<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for DescribeQuorumResponse<'a> {
    fn default() -> Self {
        Self {
            error_code: 0i16,
            error_message: None,
            topics: Vec::new(),
            nodes: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> DescribeQuorumResponse<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_quorum_response::DescribeQuorumResponse {
        crate::owned::describe_quorum_response::DescribeQuorumResponse {
            error_code: (self.error_code),
            error_message: (self.error_message).map(|s| s.to_string()),
            topics: (self.topics).iter().map(|it| it.to_owned()).collect(),
            nodes: (self.nodes).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for DescribeQuorumResponse<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        if version >= 0 { put_i16(buf, self.error_code) }
        if version >= 2 { if flex { put_compact_nullable_string(buf, self.error_message) } else { put_nullable_string(buf, self.error_message) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.topics).len(), flex); for it in &self.topics { it.encode(buf, version)?; } } }
        if version >= 2 { { crate::primitives::array::put_array_len(buf, (self.nodes).len(), flex); for it in &self.nodes { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 { n += 2; }
        if version >= 2 { n += if flex { compact_nullable_string_len(self.error_message) } else { nullable_string_len(self.error_message) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.topics).len(), flex); let body: usize = (self.topics).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 2 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.nodes).len(), flex); let body: usize = (self.nodes).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for DescribeQuorumResponse<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 { out.error_code = get_i16(buf)?; }
        if version >= 2 { out.error_message = if flex { get_compact_nullable_string_borrowed(buf)? } else { get_nullable_string_borrowed(buf)? }; }
        if version >= 0 { out.topics = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(TopicData::decode_borrow(buf, version)?); } v }; }
        if version >= 2 { out.nodes = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(Node::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct TopicData<'a> {
    pub topic_name: &'a str,
    pub partitions: Vec<PartitionData<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for TopicData<'a> {
    fn default() -> Self {
        Self {
            topic_name: "",
            partitions: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> TopicData<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_quorum_response::TopicData {
        crate::owned::describe_quorum_response::TopicData {
            topic_name: (self.topic_name).to_string(),
            partitions: (self.partitions).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for TopicData<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { if flex { put_compact_string(buf, self.topic_name) } else { put_string(buf, self.topic_name) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.partitions).len(), flex); for it in &self.partitions { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(self.topic_name) } else { string_len(self.topic_name) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.partitions).len(), flex); let body: usize = (self.partitions).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for TopicData<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.topic_name = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 0 { out.partitions = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(PartitionData::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PartitionData<'a> {
    pub partition_index: i32,
    pub error_code: i16,
    pub error_message: Option<&'a str>,
    pub leader_id: i32,
    pub leader_epoch: i32,
    pub high_watermark: i64,
    pub current_voters: Vec<super::common::replica_state::ReplicaState>,
    pub observers: Vec<super::common::replica_state::ReplicaState>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for PartitionData<'a> {
    fn default() -> Self {
        Self {
            partition_index: 0i32,
            error_code: 0i16,
            error_message: None,
            leader_id: 0i32,
            leader_epoch: 0i32,
            high_watermark: 0i64,
            current_voters: Vec::new(),
            observers: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> PartitionData<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_quorum_response::PartitionData {
        crate::owned::describe_quorum_response::PartitionData {
            partition_index: (self.partition_index),
            error_code: (self.error_code),
            error_message: (self.error_message).map(|s| s.to_string()),
            leader_id: (self.leader_id),
            leader_epoch: (self.leader_epoch),
            high_watermark: (self.high_watermark),
            current_voters: (self.current_voters).iter().map(|it| it.to_owned()).collect(),
            observers: (self.observers).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for PartitionData<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { put_i32(buf, self.partition_index) }
        if version >= 0 { put_i16(buf, self.error_code) }
        if version >= 2 { if flex { put_compact_nullable_string(buf, self.error_message) } else { put_nullable_string(buf, self.error_message) } }
        if version >= 0 { put_i32(buf, self.leader_id) }
        if version >= 0 { put_i32(buf, self.leader_epoch) }
        if version >= 0 { put_i64(buf, self.high_watermark) }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.current_voters).len(), flex); for it in &self.current_voters { it.encode(buf, version)?; } } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.observers).len(), flex); for it in &self.observers { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += 4; }
        if version >= 0 { n += 2; }
        if version >= 2 { n += if flex { compact_nullable_string_len(self.error_message) } else { nullable_string_len(self.error_message) }; }
        if version >= 0 { n += 4; }
        if version >= 0 { n += 4; }
        if version >= 0 { n += 8; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.current_voters).len(), flex); let body: usize = (self.current_voters).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.observers).len(), flex); let body: usize = (self.observers).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for PartitionData<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.partition_index = get_i32(buf)?; }
        if version >= 0 { out.error_code = get_i16(buf)?; }
        if version >= 2 { out.error_message = if flex { get_compact_nullable_string_borrowed(buf)? } else { get_nullable_string_borrowed(buf)? }; }
        if version >= 0 { out.leader_id = get_i32(buf)?; }
        if version >= 0 { out.leader_epoch = get_i32(buf)?; }
        if version >= 0 { out.high_watermark = get_i64(buf)?; }
        if version >= 0 { out.current_voters = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(super::common::replica_state::ReplicaState::decode_borrow(buf, version)?); } v }; }
        if version >= 0 { out.observers = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(super::common::replica_state::ReplicaState::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Node<'a> {
    pub node_id: i32,
    pub listeners: Vec<Listener<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for Node<'a> {
    fn default() -> Self {
        Self {
            node_id: 0i32,
            listeners: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> Node<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_quorum_response::Node {
        crate::owned::describe_quorum_response::Node {
            node_id: (self.node_id),
            listeners: (self.listeners).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for Node<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 2 { put_i32(buf, self.node_id) }
        if version >= 2 { { crate::primitives::array::put_array_len(buf, (self.listeners).len(), flex); for it in &self.listeners { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 2 { n += 4; }
        if version >= 2 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.listeners).len(), flex); let body: usize = (self.listeners).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for Node<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 2 { out.node_id = get_i32(buf)?; }
        if version >= 2 { out.listeners = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(Listener::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Listener<'a> {
    pub name: &'a str,
    pub host: &'a str,
    pub port: u16,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for Listener<'a> {
    fn default() -> Self {
        Self {
            name: "",
            host: "",
            port: 0u16,
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> Listener<'a> {
    pub fn to_owned(&self) -> crate::owned::describe_quorum_response::Listener {
        crate::owned::describe_quorum_response::Listener {
            name: (self.name).to_string(),
            host: (self.host).to_string(),
            port: (self.port),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for Listener<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 2 { if flex { put_compact_string(buf, self.name) } else { put_string(buf, self.name) } }
        if version >= 2 { if flex { put_compact_string(buf, self.host) } else { put_string(buf, self.host) } }
        if version >= 2 { put_u16(buf, self.port) }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 2 { n += if flex { compact_string_len(self.name) } else { string_len(self.name) }; }
        if version >= 2 { n += if flex { compact_string_len(self.host) } else { string_len(self.host) }; }
        if version >= 2 { n += 2; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for Listener<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 2 { out.name = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 2 { out.host = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 2 { out.port = get_u16(buf)?; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}
