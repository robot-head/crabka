// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use bytes::BufMut;
use crate::primitives::string_bytes::{
    compact_nullable_string_len, compact_string_len, nullable_string_len,
    put_compact_nullable_string, put_compact_string, put_nullable_string, put_string,
    string_len,
};
use crate::primitives::string_bytes_borrowed::{
    get_compact_nullable_string_borrowed, get_compact_string_borrowed,
    get_nullable_string_borrowed, get_string_borrowed,
};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{DecodeBorrow, Encode, ProtocolError, UnknownTaggedFields};

pub const API_KEY: i16 = 13;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 5;
pub const FLEXIBLE_MIN: i16 = 4;

#[inline]
fn is_flexible(version: i16) -> bool { version >= FLEXIBLE_MIN }

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct LeaveGroupRequest<'a> {
    pub group_id: &'a str,
    pub member_id: &'a str,
    pub members: Vec<MemberIdentity<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for LeaveGroupRequest<'a> {
    fn default() -> Self {
        Self {
            group_id: "",
            member_id: "",
            members: Vec::new(),
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> LeaveGroupRequest<'a> {
    pub fn to_owned(&self) -> crate::owned::leave_group_request::LeaveGroupRequest {
        crate::owned::leave_group_request::LeaveGroupRequest {
            group_id: (self.group_id).to_string(),
            member_id: (self.member_id).to_string(),
            members: (self.members).iter().map(|it| it.to_owned()).collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for LeaveGroupRequest<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        if version >= 0 { if flex { put_compact_string(buf, self.group_id) } else { put_string(buf, self.group_id) } }
        if version >= 0 && version <= 2 { if flex { put_compact_string(buf, self.member_id) } else { put_string(buf, self.member_id) } }
        if version >= 3 { { crate::primitives::array::put_array_len(buf, (self.members).len(), flex); for it in &self.members { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(self.group_id) } else { string_len(self.group_id) }; }
        if version >= 0 && version <= 2 { n += if flex { compact_string_len(self.member_id) } else { string_len(self.member_id) }; }
        if version >= 3 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.members).len(), flex); let body: usize = (self.members).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for LeaveGroupRequest<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion { api_key: API_KEY, version });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 { out.group_id = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 0 && version <= 2 { out.member_id = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 3 { out.members = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(MemberIdentity::decode_borrow(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl<'a> LeaveGroupRequest<'a> {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.group_id = "x"; }
        if version >= 0 && version <= 2 { m.member_id = "x"; }
        if version >= 3 { m.members = vec![MemberIdentity::populated(version)]; }
        m
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct MemberIdentity<'a> {
    pub member_id: &'a str,
    pub group_instance_id: Option<&'a str>,
    pub reason: Option<&'a str>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl<'a> Default for MemberIdentity<'a> {
    fn default() -> Self {
        Self {
            member_id: "",
            group_instance_id: None,
            reason: None,
            unknown_tagged_fields: Default::default(),
        }
    }
}

impl<'a> MemberIdentity<'a> {
    pub fn to_owned(&self) -> crate::owned::leave_group_request::MemberIdentity {
        crate::owned::leave_group_request::MemberIdentity {
            member_id: (self.member_id).to_string(),
            group_instance_id: (self.group_instance_id).map(|s| s.to_string()),
            reason: (self.reason).map(|s| s.to_string()),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}

impl<'a> Encode for MemberIdentity<'a> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 4;
        if version >= 3 { if flex { put_compact_string(buf, self.member_id) } else { put_string(buf, self.member_id) } }
        if version >= 3 { if flex { put_compact_nullable_string(buf, self.group_instance_id) } else { put_nullable_string(buf, self.group_instance_id) } }
        if version >= 5 { if flex { put_compact_nullable_string(buf, self.reason) } else { put_nullable_string(buf, self.reason) } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 4;
        let mut n: usize = 0;
        if version >= 3 { n += if flex { compact_string_len(self.member_id) } else { string_len(self.member_id) }; }
        if version >= 3 { n += if flex { compact_nullable_string_len(self.group_instance_id) } else { nullable_string_len(self.group_instance_id) }; }
        if version >= 5 { n += if flex { compact_nullable_string_len(self.reason) } else { nullable_string_len(self.reason) }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> DecodeBorrow<'de> for MemberIdentity<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 4;
        let mut out = Self::default();
        if version >= 3 { out.member_id = if flex { get_compact_string_borrowed(buf)? } else { get_string_borrowed(buf)? }; }
        if version >= 3 { out.group_instance_id = if flex { get_compact_nullable_string_borrowed(buf)? } else { get_nullable_string_borrowed(buf)? }; }
        if version >= 5 { out.reason = if flex { get_compact_nullable_string_borrowed(buf)? } else { get_nullable_string_borrowed(buf)? }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl<'a> MemberIdentity<'a> {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 3 { m.member_id = "x"; }
        if version >= 3 { m.group_instance_id = Some("x"); }
        if version >= 5 { m.reason = Some("x"); }
        m
    }
}
