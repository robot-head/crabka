// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use crate::primitives::fixed::{
    get_i8, get_i16, get_i32, get_i64, put_i8, put_i16, put_i32, put_i64,
};
use crate::primitives::string_bytes::{
    compact_nullable_string_len, get_compact_nullable_string_owned, get_nullable_string_owned,
    nullable_string_len, put_compact_nullable_string, put_nullable_string,
};
use crate::tagged_fields::{WriteTaggedFields, read_tagged_fields, tagged_fields_len};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};
use bytes::{Buf, BufMut};
pub const API_KEY: i16 = 84;
pub const MIN_VERSION: i16 = 0;
pub const MAX_VERSION: i16 = 0;
pub const FLEXIBLE_MIN: i16 = 0;
#[inline]
fn is_flexible(version: i16) -> bool {
    version >= FLEXIBLE_MIN
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct ReadShareGroupStateResponse {
    pub results: Vec<ReadStateResult>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for ReadShareGroupStateResponse {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion {
                api_key: API_KEY,
                version,
            });
        }
        let flex = is_flexible(version);
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.results).len(), flex);
                for it in &self.results {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = is_flexible(version);
        let mut n: usize = 0;
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.results).len(), flex);
                let body: usize = (self.results)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for ReadShareGroupStateResponse {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        if !(MIN_VERSION..=MAX_VERSION).contains(&version) {
            return Err(ProtocolError::UnsupportedVersion {
                api_key: API_KEY,
                version,
            });
        }
        let flex = is_flexible(version);
        let mut out = Self::default();
        if version >= 0 {
            out.results = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(ReadStateResult::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl ReadShareGroupStateResponse {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.results = vec![ReadStateResult::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct ReadStateResult {
    pub topic_id: crate::primitives::uuid::Uuid,
    pub partitions: Vec<PartitionResult>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for ReadStateResult {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 {
            crate::primitives::uuid::put_uuid(buf, self.topic_id);
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.partitions).len(), flex);
                for it in &self.partitions {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 {
            n += 16;
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.partitions).len(), flex);
                let body: usize = (self.partitions)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for ReadStateResult {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 {
            out.topic_id = crate::primitives::uuid::get_uuid(buf)?;
        }
        if version >= 0 {
            out.partitions = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(PartitionResult::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl ReadStateResult {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.topic_id = crate::primitives::uuid::Uuid([1u8; 16]);
        }
        if version >= 0 {
            m.partitions = vec![PartitionResult::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct PartitionResult {
    pub partition: i32,
    pub error_code: i16,
    pub error_message: Option<String>,
    pub state_epoch: i32,
    pub start_offset: i64,
    pub state_batches: Vec<StateBatch>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for PartitionResult {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 {
            put_i32(buf, self.partition);
        }
        if version >= 0 {
            put_i16(buf, self.error_code);
        }
        if version >= 0 {
            if flex {
                put_compact_nullable_string(buf, self.error_message.as_deref());
            } else {
                put_nullable_string(buf, self.error_message.as_deref());
            }
        }
        if version >= 0 {
            put_i32(buf, self.state_epoch);
        }
        if version >= 0 {
            put_i64(buf, self.start_offset);
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.state_batches).len(), flex);
                for it in &self.state_batches {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 2;
        }
        if version >= 0 {
            n += if flex {
                compact_nullable_string_len(self.error_message.as_deref())
            } else {
                nullable_string_len(self.error_message.as_deref())
            };
        }
        if version >= 0 {
            n += 4;
        }
        if version >= 0 {
            n += 8;
        }
        if version >= 0 {
            n += {
                let prefix = crate::primitives::array::array_len_prefix_len(
                    (self.state_batches).len(),
                    flex,
                );
                let body: usize = (self.state_batches)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for PartitionResult {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 {
            out.partition = get_i32(buf)?;
        }
        if version >= 0 {
            out.error_code = get_i16(buf)?;
        }
        if version >= 0 {
            out.error_message = if flex {
                get_compact_nullable_string_owned(buf)?
            } else {
                get_nullable_string_owned(buf)?
            };
        }
        if version >= 0 {
            out.state_epoch = get_i32(buf)?;
        }
        if version >= 0 {
            out.start_offset = get_i64(buf)?;
        }
        if version >= 0 {
            out.state_batches = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(StateBatch::decode(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl PartitionResult {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.partition = 1i32;
        }
        if version >= 0 {
            m.error_code = 1i16;
        }
        if version >= 0 {
            m.error_message = Some("x".to_string());
        }
        if version >= 0 {
            m.state_epoch = 1i32;
        }
        if version >= 0 {
            m.start_offset = 1i64;
        }
        if version >= 0 {
            m.state_batches = vec![StateBatch::populated(version)];
        }
        m
    }
}
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct StateBatch {
    pub first_offset: i64,
    pub last_offset: i64,
    pub delivery_state: i8,
    pub delivery_count: i16,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Encode for StateBatch {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 {
            put_i64(buf, self.first_offset);
        }
        if version >= 0 {
            put_i64(buf, self.last_offset);
        }
        if version >= 0 {
            put_i8(buf, self.delivery_state);
        }
        if version >= 0 {
            put_i16(buf, self.delivery_count);
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 {
            n += 8;
        }
        if version >= 0 {
            n += 8;
        }
        if version >= 0 {
            n += 1;
        }
        if version >= 0 {
            n += 2;
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl Decode<'_> for StateBatch {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 {
            out.first_offset = get_i64(buf)?;
        }
        if version >= 0 {
            out.last_offset = get_i64(buf)?;
        }
        if version >= 0 {
            out.delivery_state = get_i8(buf)?;
        }
        if version >= 0 {
            out.delivery_count = get_i16(buf)?;
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl StateBatch {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.first_offset = 1i64;
        }
        if version >= 0 {
            m.last_offset = 1i64;
        }
        if version >= 0 {
            m.delivery_state = 1i8;
        }
        if version >= 0 {
            m.delivery_count = 1i16;
        }
        m
    }
}
#[doc = " Default JSON payload matching `Self::default()` for JVM oracle differential testing."]
#[doc = " Only includes fields valid for the given version."]
#[must_use]
#[allow(unused_comparisons)]
pub fn default_json(version: i16) -> ::serde_json::Value {
    let mut obj = ::serde_json::Map::new();
    obj.insert("results".to_string(), ::serde_json::Value::Array(vec![]));
    ::serde_json::Value::Object(obj)
}
