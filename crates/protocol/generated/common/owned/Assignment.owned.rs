// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.

use bytes::{Buf, BufMut};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};

#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct Assignment {
    pub active_tasks: Vec<super::task_ids::TaskIds>,
    pub standby_tasks: Vec<super::task_ids::TaskIds>,
    pub warmup_tasks: Vec<super::task_ids::TaskIds>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Encode for Assignment {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.active_tasks).len(), flex); for it in &self.active_tasks { it.encode(buf, version)?; } } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.standby_tasks).len(), flex); for it in &self.standby_tasks { it.encode(buf, version)?; } } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.warmup_tasks).len(), flex); for it in &self.warmup_tasks { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.active_tasks).len(), flex); let body: usize = (self.active_tasks).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.standby_tasks).len(), flex); let body: usize = (self.standby_tasks).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.warmup_tasks).len(), flex); let body: usize = (self.warmup_tasks).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> Decode<'de> for Assignment {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 { out.active_tasks = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(super::task_ids::TaskIds::decode(buf, version)?); } v }; }
        if version >= 0 { out.standby_tasks = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(super::task_ids::TaskIds::decode(buf, version)?); } v }; }
        if version >= 0 { out.warmup_tasks = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(super::task_ids::TaskIds::decode(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}

#[cfg(test)]
impl Assignment {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 { m.active_tasks = vec![super::task_ids::TaskIds::populated(version)]; }
        if version >= 0 { m.standby_tasks = vec![super::task_ids::TaskIds::populated(version)]; }
        if version >= 0 { m.warmup_tasks = vec![super::task_ids::TaskIds::populated(version)]; }
        m
    }
}
