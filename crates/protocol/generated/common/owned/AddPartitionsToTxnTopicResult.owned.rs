// AUTO-GENERATED by crabka-protocol-codegen against a18251bae0b825c69794a50dffd4c3100cf5ca5b. Do not edit.

use bytes::{Buf, BufMut};
use crate::primitives::string_bytes::{
    compact_string_len, get_compact_string_owned, get_string_owned,
    put_compact_string, put_string, string_len,
};
use crate::tagged_fields::{read_tagged_fields, tagged_fields_len, WriteTaggedFields};
use crate::{Decode, Encode, ProtocolError, UnknownTaggedFields};

#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct AddPartitionsToTxnTopicResult {
    pub name: String,
    pub results_by_partition: Vec<super::add_partitions_to_txn_partition_result::AddPartitionsToTxnPartitionResult>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}

impl Encode for AddPartitionsToTxnTopicResult {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 3;
        if version >= 0 { if flex { put_compact_string(buf, &self.name) } else { put_string(buf, &self.name) } }
        if version >= 0 { { crate::primitives::array::put_array_len(buf, (self.results_by_partition).len(), flex); for it in &self.results_by_partition { it.encode(buf, version)?; } } }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 3;
        let mut n: usize = 0;
        if version >= 0 { n += if flex { compact_string_len(&self.name) } else { string_len(&self.name) }; }
        if version >= 0 { n += { let prefix = crate::primitives::array::array_len_prefix_len((self.results_by_partition).len(), flex); let body: usize = (self.results_by_partition).iter().map(|it| it.encoded_len(version)).sum(); prefix + body }; }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}

impl<'de> Decode<'de> for AddPartitionsToTxnTopicResult {
    fn decode<B: Buf>(buf: &mut B, version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 3;
        let mut out = Self::default();
        if version >= 0 { out.name = if flex { get_compact_string_owned(buf)? } else { get_string_owned(buf)? }; }
        if version >= 0 { out.results_by_partition = { let n = crate::primitives::array::get_array_len(buf, flex)?; let mut v = Vec::with_capacity(n); for _ in 0..n { v.push(super::add_partitions_to_txn_partition_result::AddPartitionsToTxnPartitionResult::decode(buf, version)?); } v }; }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| {
                Ok(false)
            })?;
        }
        Ok(out)
    }
}
