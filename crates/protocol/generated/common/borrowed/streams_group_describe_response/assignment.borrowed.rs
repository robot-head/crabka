// AUTO-GENERATED by crabka-protocol-codegen against a9ce3221537b8653448750697915607dc7936cf3. Do not edit.
use crate::tagged_fields::{WriteTaggedFields, read_tagged_fields, tagged_fields_len};
use crate::{DecodeBorrow, Encode, ProtocolError, UnknownTaggedFields};
use bytes::BufMut;
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct Assignment<'a> {
    pub active_tasks: Vec<super::task_ids::TaskIds<'a>>,
    pub standby_tasks: Vec<super::task_ids::TaskIds<'a>>,
    pub warmup_tasks: Vec<super::task_ids::TaskIds<'a>>,
    pub unknown_tagged_fields: UnknownTaggedFields,
}
impl Assignment<'_> {
    pub fn to_owned(
        &self,
    ) -> crate::owned::common::streams_group_describe_response::assignment::Assignment {
        crate::owned::common::streams_group_describe_response::assignment::Assignment {
            active_tasks: (self.active_tasks)
                .iter()
                .map(super::task_ids::TaskIds::to_owned)
                .collect(),
            standby_tasks: (self.standby_tasks)
                .iter()
                .map(super::task_ids::TaskIds::to_owned)
                .collect(),
            warmup_tasks: (self.warmup_tasks)
                .iter()
                .map(super::task_ids::TaskIds::to_owned)
                .collect(),
            unknown_tagged_fields: self.unknown_tagged_fields.clone(),
        }
    }
}
impl Encode for Assignment<'_> {
    fn encode<B: BufMut>(&self, buf: &mut B, version: i16) -> Result<(), ProtocolError> {
        let flex = version >= 0;
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.active_tasks).len(), flex);
                for it in &self.active_tasks {
                    it.encode(buf, version)?;
                }
            }
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.standby_tasks).len(), flex);
                for it in &self.standby_tasks {
                    it.encode(buf, version)?;
                }
            }
        }
        if version >= 0 {
            {
                crate::primitives::array::put_array_len(buf, (self.warmup_tasks).len(), flex);
                for it in &self.warmup_tasks {
                    it.encode(buf, version)?;
                }
            }
        }
        if flex {
            let tagged = WriteTaggedFields::new();
            tagged.write(buf, &self.unknown_tagged_fields);
        }
        Ok(())
    }
    fn encoded_len(&self, version: i16) -> usize {
        let flex = version >= 0;
        let mut n: usize = 0;
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.active_tasks).len(), flex);
                let body: usize = (self.active_tasks)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if version >= 0 {
            n += {
                let prefix = crate::primitives::array::array_len_prefix_len(
                    (self.standby_tasks).len(),
                    flex,
                );
                let body: usize = (self.standby_tasks)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if version >= 0 {
            n += {
                let prefix =
                    crate::primitives::array::array_len_prefix_len((self.warmup_tasks).len(), flex);
                let body: usize = (self.warmup_tasks)
                    .iter()
                    .map(|it| it.encoded_len(version))
                    .sum();
                prefix + body
            };
        }
        if flex {
            let known_pairs: Vec<(u32, usize)> = Vec::new();
            n += tagged_fields_len(&known_pairs, &self.unknown_tagged_fields);
        }
        n
    }
}
impl<'de> DecodeBorrow<'de> for Assignment<'de> {
    fn decode_borrow(buf: &mut &'de [u8], version: i16) -> Result<Self, ProtocolError> {
        let flex = version >= 0;
        let mut out = Self::default();
        if version >= 0 {
            out.active_tasks = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(super::task_ids::TaskIds::decode_borrow(buf, version)?);
                }
                v
            };
        }
        if version >= 0 {
            out.standby_tasks = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(super::task_ids::TaskIds::decode_borrow(buf, version)?);
                }
                v
            };
        }
        if version >= 0 {
            out.warmup_tasks = {
                let n = crate::primitives::array::get_array_len(buf, flex)?;
                let mut v = Vec::with_capacity(n);
                for _ in 0..n {
                    v.push(super::task_ids::TaskIds::decode_borrow(buf, version)?);
                }
                v
            };
        }
        if flex {
            out.unknown_tagged_fields = read_tagged_fields(buf, |_tag, _payload| Ok(false))?;
        }
        Ok(out)
    }
}
#[cfg(test)]
impl Assignment<'_> {
    #[must_use]
    pub fn populated(version: i16) -> Self {
        let mut m = Self::default();
        if version >= 0 {
            m.active_tasks = vec![super::task_ids::TaskIds::populated(version)];
        }
        if version >= 0 {
            m.standby_tasks = vec![super::task_ids::TaskIds::populated(version)];
        }
        if version >= 0 {
            m.warmup_tasks = vec![super::task_ids::TaskIds::populated(version)];
        }
        m
    }
}
