load("//:bazel/rust_package.bzl", "rust_package")

rust_package(
    name = "compression",
    cargo_name = "crabka-compression",
    crate_name = "crabka_compression",
    test_binaries = {"crabka-oracle": "//tools/oracle:oracle"},
    test_sizes = {"differential": "small"},
    test_tags = {"differential": ["manual"]},
)
