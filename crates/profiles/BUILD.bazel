load("//:bazel/rust_package.bzl", "rust_package")

rust_package(
    name = "profiles",
    build_script = True,
    cargo_name = "crabka-profiles",
    crate_name = "crabka_profiles",
    protoc = True,
)
