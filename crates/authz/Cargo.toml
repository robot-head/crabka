[package]
name = "crabka-authz"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Shared Kafka-ACL authorization evaluator for the Crabka broker and gateway"
repository = "https://github.com/robot-head/crabka"
homepage = "https://github.com/robot-head/crabka"
documentation = "https://docs.rs/crabka-authz"
readme = "README.md"
keywords = ["kafka", "authorization", "acl", "security", "crabka"]
categories = ["authentication", "network-programming"]

[lints]
workspace = true

[dependencies]
assert2 = { workspace = true }
crabka-metadata = { version = "0.4.0", path = "../metadata" }
crabka-security = { version = "0.4.0", path = "../security" }
tracing = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
uuid = { workspace = true }
