[package]
name = "observability-demo-app"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Instrumented orders-analytics Kafka-Streams demo app for the Crabka observability demo"

[lints]
workspace = true

[[bin]]
name = "observability-demo-app"
path = "src/main.rs"

[features]
heap-profiling = ["crabka-telemetry/heap-profiling"]

[dependencies]
assert2 = { workspace = true }
crabka-client-core = { version = "0.3.9", path = "../client-core" }
crabka-client-streams = { version = "0.3.9", path = "../client-streams" }
crabka-client-producer = { version = "0.3.9", path = "../client-producer" }
crabka-client-consumer = { version = "0.3.9", path = "../client-consumer" }
crabka-schema-serde = { version = "0.3.9", path = "../schema-serde", features = ["protobuf"] }
crabka-telemetry = { version = "0.3.8", path = "../telemetry" }
axum = { workspace = true }
bytes = { workspace = true }
clap = { workspace = true, features = ["derive", "env"] }
prometheus-client = { workspace = true }
refined_type = { workspace = true }
prost = { workspace = true }
prost-reflect = { workspace = true, features = ["derive"] }
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "time", "signal", "sync"] }
tracing = { workspace = true }

[build-dependencies]
protox = { workspace = true }
prost-build = { workspace = true }
prost-reflect = { workspace = true, features = ["derive"] }

[dev-dependencies]
