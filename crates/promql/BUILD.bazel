load("//:bazel/rust_package.bzl", "rust_package")

rust_package(
    name = "promql",
    cargo_name = "crabka-promql",
    crate_name = "crabka_promql",
    harnessless = ["conformance"],
    test_sizes = {"conformance": "small"},
)
